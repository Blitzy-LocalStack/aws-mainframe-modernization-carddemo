# =============================================================================
# services/reporting-service/src/main/resources/application-prod.yml
# -----------------------------------------------------------------------------
# Purpose:
#   The production profile overlay for the CardDemo reporting bounded context. It
#   overrides VALUES ONLY, on keys application.yml beside it already declares, and
#   introduces no key absent there. Six leaf values are set, across the only axes a
#   profile in this project may vary: connection-pool sizing including the
#   acquisition timeout, the streaming fetch size, and log verbosity. Nothing here
#   changes topology or security posture.
#
#   Assumptions: three of the six values RESTATE a base value rather than changing
#   it, deliberately in each case -- application-dev.yml lowers the fetch size, the
#   root level and the project level, so restating them here is what stops a dev
#   value being read as if it applied everywhere. The other three genuinely change,
#   and all three follow from one platform fact: a production cluster has a non-zero
#   minimum capacity and therefore never pauses, whereas the dev overlay sizes its
#   pool down to zero idle connections because any connection keeps a zero-minimum
#   cluster awake.
#
#
# Parameters:
#   Each property key is the YAML analogue of a parameter. This overlay consumes no
#   environment variable of its own; the base document already builds its Parameter
#   Store and Secrets Manager prefixes from CARDDEMO_CONFIG_PREFIX, so re-declaring
#   the import list here would name those two locations in a second place.
#
# Return values:
#   The effective configuration handed to the Spring application context after this
#   overlay is merged over src/main/resources/application.yml.
#
# Exceptions or errors:
#   A misspelled Spring property is silently ignored rather than reported, so a typo
#   here does not fail startup - it leaves the base value in force and the intended
#   change never happens. Two failures here would be loud, and both come from the
#   pool: a ceiling below the concurrency a statement run needs makes requests time
#   out on connection acquisition rather than on any query, and an acquisition
#   timeout shorter than a cursor walk's first fetch fails a request that was about
#   to succeed.
#
#   - Trade-offs: this context's borrows are unlike any sibling's and the pool
#     numbers follow from that rather than from a house figure. Every borrow is a
#     cursor walk over a report or a statement run rather than a keyed read, so one
#     connection is held for the length of a whole document; sizing this pool like a
#     request-response context's would let a handful of concurrent statement runs
#     occupy every connection while each waits on the same cursor.
#   - Alternatives Considered: declaring management.endpoint.health.show-details in
#     this overlay. Rejected -- recorded here because an absent key has no line of
#     its own to sit beside. Restating `never` would assert a decision this file did
#     not make, the base having already set it and stated that the development
#     overlay may widen it and production does not. `when-authorized` would return
#     the same bare status as `never` while appearing configured, because both
#     pollers of that path are unauthenticated by necessity -- the load balancer
#     target group and the container health check, neither of which can present a
#     token. Inheriting buys bounded disclosure: a detail payload enumerates the
#     schemas the connection reaches, and this context reaches read-only views
#     across every other bounded context's schema, so the body would sketch the
#     whole data-tier layout. What it gives up is that a failing probe answers with
#     a bare status, so the datasource contributor has to be read from the metrics
#     scrape path or the task's own logs, neither reachable without a credential.
# =============================================================================

spring:
  datasource:
    hikari:
      # Trade-offs: eight connections rather than the base document's four,
      #       and deliberately not the twenty a request-response sibling uses. Each
      #       borrow here is held for a whole report or statement run rather than for
      #       a statement, so eight concurrent document generations is a realistic
      #       production ceiling while twenty would let this one context claim a
      #       disproportionate share of a budget shared with seven peers - auth,
      #       account, card, transaction, reference, batch and authorization, all on
      #       the one cluster. The quantity that must fit the cluster's allowance is
      #       this number multiplied by this service's task count and then summed
      #       across all eight services, and production runs more tasks than dev, so
      #       a per-task figure that reads as modest is the one that multiplies.
      #       (2) Assumptions: this pool is the read side of a SELECT-only database
      #       role, so widening it cannot increase write contention; what it spends
      #       is connection slots and the per-cursor heap recorded on the fetch size
      #       below.
      #       (3) Alternatives Considered: leaving the base four in place and scaling
      #       the task count instead. Rejected because a report request is answered
      #       by whichever task the load balancer chose, so a task whose pool is full
      #       queues the request rather than shedding it to a task with capacity; the
      #       queue forms per task, and widening the pool is what shortens it.
      maximum-pool-size: 8

      # Trade-offs: a floor of two rather than the base document's one and
      #       against the dev overlay's zero. Production capacity has a non-zero
      #       minimum so the cluster never pauses, which removes the reason dev
      #       drains to empty; what remains is the cost of a fresh handshake inside
      #       the first fetch of a report an operator is waiting on, and two covers
      #       the steady-state concurrency of the report and statement paths so only
      #       a genuine burst pays it.
      #       (2) Assumptions: this value is what the dev overlay's zero is measured
      #       against, and the two are not interchangeable. Dev sets zero because any
      #       held connection keeps a zero-minimum cluster awake for the life of the
      #       task; that argument has no force here, and applying it would trade a
      #       real latency cost for a saving that does not exist.
      minimum-idle: 2

      # Trade-offs: ten seconds rather than the base document's thirty. The
      #       base is sized generously because the same value has to contain a
      #       zero-minimum cluster's roughly fifteen-second resume, which is a dev
      #       concern only. In production the longer wait is actively harmful, and
      #       the harm is a cascade rather than a slow request: during pool
      #       exhaustion each waiting request occupies its servlet thread for the
      #       whole timeout, the thread pool saturates, the health endpoint is served
      #       by that same pool and stops answering, the load balancer deregisters
      #       the task, and a database slowdown has become a task replacement loop.
      #       (2) Trade-offs: the accepted cost is that a genuinely slow but
      #       recovering database sheds report requests it would eventually have
      #       served. A shed request returns an error an operator can retry; a
      #       replaced task loses every in-flight document at once.
      connection-timeout: 10000

  jpa:
    # Assumptions: everything under `properties` is an UNTYPED passthrough map
    #   handed verbatim to the persistence provider, not a set of typed framework
    #   properties, so nothing validates these key names -- relaxed binding and
    #   configuration metadata do not apply here. Two consequences are specific
    #   to a delta file and neither reports itself. The provider's own spelling
    #   must be used exactly: it is fetch_size with an UNDERSCORE, and
    #   "correcting" it to the kebab-case fetch-size that the rest of this
    #   document uses would bind nothing, leave the base value silently in force
    #   and make this override a no-op. And because the path is five levels deep,
    #   a typo anywhere along spring.jpa.properties.hibernate.jdbc has the same
    #   silent outcome rather than a startup error naming the key.
    properties:
      hibernate:
        jdbc:
          # Assumptions: two hundred, restating the base document's value
          #       against the dev overlay's hundred. Dev halves it because a dev task
          #       runs with a smaller memory reservation and the per-cursor
          #       high-water mark matters more there than the round-trip count; a
          #       production task has the reservation the base was sized for, so the
          #       base value is the right one here and is restated rather than
          #       inherited so the halving cannot be read as a system-wide setting.
          #       (2) Assumptions: THIS VALUE IS NOT A CAP AND MUST NEVER BE READ AS
          #       ONE. It bounds rows in flight per round trip; it does not bound how
          #       many transactions a statement may contain nor how many cards a run
          #       may cover. The target declares no arity of any kind, which is
          #       divergence D-2 in
          #       docs/architecture/cobol-to-service-traceability.md, and the base
          #       document records in full why two hundred was chosen to be none of
          #       the baseline's three table numbers.
          #       (3) Assumptions: both preconditions for a server-side cursor still
          #       hold unchanged - the value stays positive and the base keeps
          #       automatic commit disabled - so nothing here reintroduces the
          #       client-side materialisation the setting exists to avoid.
          fetch_size: 200

logging:
  level:
    # Assumptions: WARN, restating the base document's value. Dev raises the
    #   root logger to INFO for one specific reason recorded there - the base
    #   imports its configuration with the optional prefix, so a wrong prefix
    #   resolves nothing silently and only the framework's own configuration
    #   line reports it. That diagnostic is worth an environment's log volume in
    #   dev and is not worth it at production request rates, where root INFO is
    #   framework narration on every call and log ingestion is billed by the
    #   gigabyte.
    root: WARN

    # Trade-offs: INFO, restating the base document's value against the
    #       dev overlay's DEBUG. At INFO this context records the outcome of a report
    #       or statement request; at DEBUG it records which validation branch the
    #       request took and which preset it resolved, which is what makes the
    #       transcribed COBOL paragraph logic inspectable during development. The
    #       cost in production is unbounded in aggregate and the benefit is
    #       retrospective.
    #       (2) Assumptions: this is restated rather than inherited because it is the
    #       value an incident most tempts an operator to raise permanently. A
    #       deliberate temporary raise belongs in a task-definition override, where
    #       it expires with the deployment, rather than in this file where it would
    #       become the new floor.
    #       (3) Alternatives Considered: matching the dev overlay's DEBUG, and going
    #       further to lower a persistence SQL category as well. Both rejected on a
    #       ground specific to THIS context rather than on a house preference. Log
    #       ingestion is billed per ingested gigabyte, and every query this service
    #       runs is a STREAMING cursor walk, so a statement- or row-level category
    #       emits one line per fetched row: a single large statement run would
    #       dominate the log bill for the whole environment, and it would do so
    #       exactly when an operator was least able to afford the noise. The
    #       diagnostic those levels buy is a parity-verification diagnostic, and
    #       parity is verified outside production -- against the batch golden
    #       masters under tests/golden/reporting and tests/golden/statement for the
    #       report and statement paths, and by transcription against the cited
    #       program lines for everything the goldens do not reach -- rather than by
    #       reading a production log.
    #       (4) Assumptions: INFO stays traceable across services without any of
    #       that, because CorrelationIdFilter in com.carddemo.common.web publishes
    #       correlationId and requestId into the mapped diagnostic context and the
    #       shared console pattern renders both on every line. One request can
    #       therefore be followed across all eight services at INFO, which is what
    #       makes the row-level detail above genuinely optional rather than merely
    #       unaffordable.
    com.carddemo: INFO

# =============================================================================
# Closing notes: five deliberate absences
# -----------------------------------------------------------------------------
#   Each entry is a key a maintainer would reasonably expect in a production
#   overlay, recorded together because an absent key has no line of its own to sit
#   beside.
#
# Assumptions: no spring.config.import. A profile's import list REPLACES the base
#   document's rather than adding to it, and the base declares three entries, so
#   declaring the key here to change nothing would restate all three and introduce a
#   second place for any of them to be edited. Sibling contexts declare it because
#   their overlays add a location; this one has nothing to add.
#
# Alternatives Considered: an Aurora reader endpoint, a read replica or a routing
#   datasource, so a statement run's cursor walk never touches the writer. This is
#   the absence a production overlay is most likely to be challenged on. Rejected on
#   the ground the migration plan states directly, that a replica adds cost and
#   replica-lag semantics for no parity benefit, and the lag half is operative here:
#   these views are read immediately after the nightly posting and interest steps
#   commit, so a lagging reader would render a statement from balances correct for no
#   point in time the baseline can produce, and golden-master parity would fail in a
#   way that looks like an arithmetic defect rather than a topology one. Choosing an
#   endpoint would also be a TOPOLOGY change, outside the sizing, retention and
#   verbosity axes a profile may vary, so it belongs to the base document and to
#   infra/modules/aurora-postgresql together.
#
# Assumptions: no spring.datasource.hikari.idle-timeout or max-lifetime override.
#   Neither is an environment question - the idle ceiling releases connections the
#   pool grew during a burst, and the lifetime ceiling is deliberately shorter than
#   the cluster's own idle timeout so the pool retires a connection on its own terms
#   rather than discovering it closed midway through a cursor walk.
#
# Assumptions: no spring.datasource.hikari.read-only or auto-commit override. Both
#   are behavioural rather than environmental: the first is the client half of a
#   SELECT-only posture whose server half is the database role's grants, and the
#   second is a precondition for the server-side cursor the fetch size above depends
#   on. A profile changing either would stop being the same service configured
#   differently, which is what the sizing, retention and verbosity limit prevents.
#
# Assumptions: no management.endpoints.web.exposure.include override, and no
#   spring.sql.init.mode override. The base already narrows the actuator surface and
#   pins initialisation to never - the latter because this module owns no relational
#   object, so a schema.sql or data.sql arriving by copy from a peer would be
#   executed against a role holding SELECT-only privileges.
# =============================================================================
