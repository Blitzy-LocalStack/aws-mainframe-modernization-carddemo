# =============================================================================
# services/auth-service/src/main/resources/openapi/auth-api.yaml
# -----------------------------------------------------------------------------
# Purpose: this document is the committed OpenAPI 3.1 contract of record for the
#       CardDemo auth bounded context. Everything that touches this context is
#       written against it rather than against a reading of the baseline, and when
#       controllers exist here the document springdoc serves at /v3/api-docs must
#       agree with it. A difference between the two is a defect in whichever one
#       moved; it is not a tolerated variation and it is not something to reconcile
#       at runtime. Every artifact this contract governs is present, and each is
#       held to it mechanically rather than by review: config/SecurityConfig.java
#       enforces the authority each operation below declares; api/AuthController.java
#       and api/UserController.java serve the operations; the request and response
#       shapes are the thirteen types under dto/, into which mapper/UserMapper.java
#       translates the stored row; config/AuthApiContractTest.java compares the served
#       route set and the declared authorities against this document in BOTH
#       directions, so a published operation with no handler and a handler this
#       document omits both fail; and the browser client ui/src/api/auth.ts calls
#       these operations, which ui/src/api/contracts.test.ts asserts against this same
#       document. The contract covers nine operations: the sign-on token exchange
#       migrated from app/cbl/COSGN00C.cbl, the challenge exchange that completes it
#       for a user whose password is temporary, the token renewal that has no baseline
#       counterpart, the session revocation that has none either, and the four
#       user-administration operations migrated from
#       app/cbl/COUSR00C.cbl, COUSR01C.cbl, COUSR02C.cbl and COUSR03C.cbl, whose
#       shared list view contributes the fifth of those.
#
# Structure: purpose for every top-level block is stated here, in the one place a
#       format with no docstring construct has available, so that each block below
#       carries rationale and nothing else.
#       - servers: the single server entry, declared as a substitutable variable
#         rather than as any environment's address.
#       - security: the bearer scheme, applied to every operation by default.
#       - paths: the nine operations of this context, in the order a user meets
#         them -- sign on, answer a sign-on challenge, renew a token set, sign out,
#         then list users, create one, read one, update one, delete one. The first
#         four require no token and the last five are administrative, which is the
#         split the operation census asserts.
#       - components.securitySchemes: the single authentication scheme every
#         operation but the four Sign-On ones requires -- sign-on, challenge,
#         refresh and sign-out each declare an empty security requirement, because
#         a caller cannot present the token the first three exist to issue or renew,
#         and the fourth is authorised by the very token it revokes.
#       - components.headers: the response header every operation returns,
#         declared once.
#       - components.responses: the two error responses that are identical across
#         all five user-administration operations.
#       - components.schemas: twenty-one payload shapes -- ELEVEN of this context's
#         own and TEN that mirror types the shared kernel owns, among the latter the
#         error body every failing operation returns together with the two nested
#         shapes it carries. Refactoring Rationale: this read "six of its own, and
#         five that mirror", which was a count of neither half; it was re-measured
#         while adding the sign-out request rather than incremented, because an
#         inventory that was wrong before the addition stays wrong after it.
#
# WHY (non-obvious design decisions):
#       (0) Assumptions: components.parameters declares one entry, the correlation
#       header every operation accepts, referenced rather than re-typed at each
#       operation.
#       (1) Assumptions: the comment conventions in this file. Purpose is stated
#       once, in this header block, which is the analogue of a docstring for a
#       format that has none; no statement-level "# WHAT:" comment sits above any
#       key below. Rationale attached to a single element uses a labelled comment
#       at that element's own indentation, continued at the marker plus three
#       spaces, matching the sibling application.yml and db/migration/V1__auth.sql
#       in this same resources tree, while a block-level statement continues at the
#       marker plus seven spaces so that a reader can tell the scope of a comment
#       from its shape alone. Only four rationale labels appear anywhere in this
#       file -- Alternatives Considered, Refactoring Rationale, Assumptions and
#       Trade-offs -- because those are the four the project's Explainability
#       rule enumerates, and mixing in a variant spelling would make the set
#       unsearchable. Trade-offs: describing each block up here costs the reader a
#       jump from a block back to the header, and buys the removal of a line above
#       every block that restated the key beneath it, which the "The `# WHAT:` /
#       `# WHY :` idiom -- prose command blocks only" section of
#       docs/CODE_DOCUMENTATION_STANDARD.md forbids outright in a .yaml file.
#       (2) Refactoring Rationale: the five user-administration operations are
#       published under /api/v1/auth/users rather than under a top-level
#       /api/v1/users collection, and this is the one place this contract's paths
#       differ from the figure quoted in the migration plan. A top-level
#       /api/v1/users prefix is not reachable on the provisioned environment: the
#       edge publishes seven context prefixes, and auth-service's is /api/v1/auth
#       alone -- see the ALB rule paths at infra/envs/dev/main.tf line 72 and
#       infra/envs/prod/main.tf line 72, and the route-key list at
#       infra/modules/api-gateway-http/variables.tf lines 375 to 376. The same
#       module states the intended arrangement outright at lines 634 to 637:
#       POST /api/v1/auth/signon is unauthenticated, and every other path under
#       /api/v1/auth, "including the user administration endpoints", stays on the
#       authorized route. Publishing a prefix the load balancer has no rule for
#       would produce a contract whose operations answer nowhere, which the
#       acceptance criterion that the business flows work end to end on the
#       provisioned environment does not permit. Every method, authority, schema
#       and message text below is exactly as specified; only the reachable prefix
#       is used.
#       (3) Assumptions: all FOUR unauthenticated /auth routes are contracted
#       here -- POST /api/v1/auth/signon, POST /api/v1/auth/refresh,
#       POST /api/v1/auth/challenge and POST /api/v1/auth/signout -- so this
#       document is the whole edge surface for /auth and a reader comparing it
#       against the route table finds no gap. Those four are exactly the keys
#       validated at infra/modules/api-gateway-http/variables.tf, so the gateway
#       forwards no operation this document does not describe.
#       Assumptions: the challenge in particular is not an optional extra flow but
#       the FIRST thing every provisioned user does, because the infrastructure
#       seeds each user with a temporary password
#       (infra/modules/cognito/variables.tf, temporary_password_validity_days).
#       Trade-offs: omitting it would not merely leave a flow undescribed -- the
#       outcome would route to 500, so the very first sign-on of every provisioned
#       user would report a server fault with no path to set a permanent password.
#       Its shape is the pool's own documented challenge exchange rather than a
#       guess, and its members are named on SignOnChallenge and
#       SignOnChallengeRequest below.
#       (4) Refactoring Rationale: no password is carried anywhere except the
#       sign-on request, which forwards it to the identity provider and keeps
#       nothing. The baseline handled the credential three ways and this contract
#       ends all three: it was stored in the record, at
#       app/cpy/CSUSR01Y.cpy line 21 ("05 SEC-USR-PWD PIC X(08)."); it was
#       compared in the clear, at app/cbl/COSGN00C.cbl line 223
#       ("IF SEC-USR-PWD = WS-USER-PWD"); and it was written back onto the 3270
#       screen, at app/cbl/COUSR02C.cbl line 169
#       ("MOVE SEC-USR-PWD TO PASSWDI OF COUSR2AI"), so the update screen
#       displayed every user's password to whoever opened it. The sibling
#       migration db/migration/V1__auth.sql creates no password column, so there
#       is nothing for a create, update or read operation here to accept or
#       return. This is the one place where fidelity to the baseline is declined
#       rather than preserved, and it is declined openly.
#       (5) Assumptions: this contract is not verifiable by the repository's
#       golden-master suite. tests/README.md lines 83 to 85 record that the
#       online CO* CICS programs cannot run end to end without a CICS runtime,
#       which is absent on the runner, and that only their extractable
#       field-validation logic is unit-tested. That glob covers all five auth
#       programs, so no executable oracle exists for the HTTP-facing behaviour
#       described here and none is claimed. What IS directly verifiable is the
#       message text: every literal quoted below was read from the cited program
#       line and is reproduced character for character, so this document is
#       itself the assertion point for message fidelity.
#       (6) Assumptions: message strings are carried literally, one at a time,
#       with no normalising rule applied to any of them. Spacing before an
#       ellipsis is not derivable and must not be guessed: COSGN00C's messages
#       carry a space before it, COUSR00C's carry none, and COUSR01C, COUSR02C
#       and COUSR03C are mixed inside a single file, their validation errors
#       carrying none while their prompts and successes carry one. Inferring a
#       house rule and applying it would silently alter observable output on
#       whichever side of the split lost, so each literal is keyed to its
#       originating program and line and copied as it stands.
#       (7) Assumptions: the observable message field in this context is 78
#       characters wide, not 75. It is "ERRMSGI PIC X(78)" in all five symbolic
#       maps -- app/cpy-bms/COSGN00.CPY line 84, COUSR00.CPY line 372,
#       COUSR01.CPY line 90, COUSR02.CPY line 90 and COUSR03.CPY line 84 -- and
#       none of these five programs copies the templated field-highlight book, so
#       the narrower 75-character figure that belongs to the shared
#       CCARD-ERROR-MSG field does not govern here. The longest string this
#       contract carries is 44 characters
#       ("You are already at the bottom of the page..." at
#       app/cbl/COUSR00C.cbl line 273), so it fits both widths with room to
#       spare and no message is ever truncated to fit a field.
#       (8) Assumptions: field widths are asserted from two independent readings
#       apiece, which is why they are stated as constraints rather than as
#       guidance. The 80-byte record at app/cpy/CSUSR01Y.cpy lines 17 to 23 gives
#       the user identifier 8, both names 20 and the type 1; the symbolic maps
#       give the same widths at app/cpy-bms/COSGN00.CPY lines 72 and 78,
#       COUSR01.CPY lines 60, 66, 72, 78 and 84, COUSR02.CPY lines 60, 66, 72, 78
#       and 84, and COUSR03.CPY lines 60, 66, 72 and 78. The A/U value set is
#       taken from app/cpy/COCOM01Y.cpy lines 26 to 28, where CDEMO-USER-TYPE
#       carries the two condition names; it is NOT taken from
#       app/cpy/CSUSR01Y.cpy line 22, which declares the field's width but
#       enumerates no values.
#       (9) Assumptions: nothing in the shared kernel is redeclared here as a
#       parallel type. ApiError, its per-field error array, its abend aggregate
#       and the keyset page envelope are described exactly as
#       services/common-lib publishes them, on the same principle the reference
#       suite states for record layouts at tests/README.md lines 540 to 542 --
#       a layout is resolved through one include path and never duplicated. A
#       second, subtly different error or paging shape declared locally would
#       compile, serve and disagree with the eight services that share the
#       originals.
# =============================================================================

openapi: 3.1.1

info:
  title: CardDemo Auth API
  version: 1.0.0
  summary: Sign-on and user administration for the CardDemo auth bounded context.
  description: >-
    Migrated surface of the CardDemo sign-on transaction and the four
    user-administration transactions that the CICS resource definition
    registered as CC00, CU00, CU01, CU02 and CU03
    (app/csd/CARDDEMO.CSD lines 378, 449, 459, 469 and 479). Sign-on exchanges a
    user identifier and password for tokens minted by the configured Cognito
    user pool, or returns the challenge a temporary password raises, which the
    challenge operation answers; the remaining five operations administer the rows
    of the auth schema's users table and are restricted to the carddemo-admin
    authority.
    Pseudo-conversational state is gone: no operation accepts or returns a
    communication area, identity is read from the signed token on every request,
    and the selected user travels in the request path.
  license:
    name: Apache-2.0
    identifier: Apache-2.0

# WHY : Assumptions: the edge address is a Terraform output consumed at deploy
#       time, never a value committed to source -- no service hard-codes an
#       endpoint. The browser client resolves the same origin from its own
#       build-time VITE_API_BASE_URL, so a literal host written here would be a
#       second, competing source for one value.
# WHY : Trade-offs: the default is the reserved, permanently unresolvable
#       example.invalid name rather than a plausible-looking hostname. A
#       plausible default is worse than an obvious placeholder, because a tool
#       that silently used it would attempt a real connection to somebody's host;
#       a name reserved never to resolve fails immediately and unmistakably, at
#       the cost of the document not being directly executable until the variable
#       is supplied.
servers:
  - url: "{edgeOrigin}"
    description: >-
      Origin of the HTTP API that fronts this service. Supplied per environment
      from infrastructure outputs; the default below is a deliberately
      unresolvable placeholder and is not an address of any deployment.
    variables:
      edgeOrigin:
        default: "https://auth-api.example.invalid"
        description: >-
          Scheme and host of the environment's API edge, without a trailing
          slash. Every path in this document is absolute and already carries the
          /api/v1 prefix that the edge route keys publish.

tags:
  - name: Sign-On
    description: >-
      The token exchange migrated from app/cbl/COSGN00C.cbl, together with the
      challenge exchange that completes it for a user whose password is temporary,
      the refresh exchange that renews an expired token set, and the sign-out
      exchange that revokes the refresh token a session renews from. All four are
      reachable without a token and all four carry x-required-authority none: three
      of them are the operations that issue a token, and the fourth is authorised by
      the token it invalidates.
  - name: User Administration
    description: >-
      List, create, read, update and delete over the auth schema's users table,
      migrated from app/cbl/COUSR00C.cbl, COUSR01C.cbl, COUSR02C.cbl and
      COUSR03C.cbl. Every operation carries x-required-authority carddemo-admin.

# WHY : Assumptions: a document-level default makes the unauthenticated
#       operations the explicit exception rather than the silent norm, so an
#       operation added later without a security block inherits the
#       authenticated posture instead of being published open.
# WHY : Assumptions: FOUR operations override this default with an empty
#       requirement -- signOn, answerSignOnChallenge, refreshTokens and signOut --
#       and they are exactly the four the Sign-On tag covers and the four
#       SecurityConfig names in its unauthenticated list. The first three are open
#       because between them they are the operations that ISSUE a token, so none of
#       them can require one: sign-on issues the first, the challenge issues the one
#       a temporary password could not, and the renewal issues a replacement for one
#       that has expired -- a caller holding only an expired access token must still
#       be able to renew. The fourth is open for the mirror of that reason: it
#       revokes a refresh token, possession of that token is the only authority a
#       caller could present for revoking it, and demanding a live access token as
#       well would refuse the revocation exactly when the session it ends has been
#       abandoned rather than closed. The five user operations inherit the default
#       and override nothing.
security:
  - bearerAuth: []

# WHY : Assumptions: the authority each operation requires is declared as a
#       MACHINE-READABLE field, and prose alone would not do. Five operations here
#       also say "Requires the carddemo-admin authority" in their description, and
#       nothing can read that: a generator, a gateway or a linter would see five
#       ordinary authenticated routes indistinguishable from any other, leaving the
#       administrative restriction an assertion rather than a contract. Every
#       operation carries x-required-authority, and
#       AuthApiContractTest asserts that the value on each one agrees with the
#       authority SecurityConfig enforces for the path it sits on, so the two
#       cannot drift apart while both continue to compile.
# WHY : Alternatives Considered: expressing the authority as a security-scheme
#       scope, by writing `security: [{bearerAuth: [carddemo-admin]}]` on each
#       administrative operation. Rejected because the specification requires the
#       scope list to be EMPTY for a scheme that is not oauth2 or openIdConnect,
#       and bearerAuth is `type: http`, so a populated list there is invalid and a
#       validating tool would reject or ignore it. Re-declaring the scheme as
#       oauth2 purely to gain a scope list was rejected in turn, because these
#       values are Cognito GROUP names carried in a cognito:groups claim rather
#       than OAuth scopes, and publishing them as scopes would state something
#       untrue about how the token is issued.
# WHY : Assumptions: three values are admitted and no more. carddemo-admin and
#       carddemo-user are exactly the two group names the shared token converter
#       recognises, ADMIN_AUTHORITY and USER_AUTHORITY in
#       services/common-lib/src/main/java/com/carddemo/common/security/JwtRoleConverter.java,
#       which are the migrated form of the two SEC-USR-TYPE values 'A' and 'U' at
#       app/cpy/CSUSR01Y.cpy line 22. The third value, none, marks an operation
#       reachable without a token and exists so that an unauthenticated operation
#       is stated rather than inferred from an absent field -- an omitted field and
#       a deliberately open operation would otherwise look identical.
x-authority-model:
  field: x-required-authority
  values:
    - none
    - carddemo-user
    - carddemo-admin
  enforcedBy: >-
    services/auth-service/src/main/java/com/carddemo/auth/config/SecurityConfig.java
  assertedBy: >-
    services/auth-service/src/test/java/com/carddemo/auth/config/AuthApiContractTest.java
# WHY : Assumptions: each operation is stateless. The baseline carried continuity
#       between screen turns in the communication area declared at
#       app/cpy/COCOM01Y.cpy lines 19 to 44, and that structure decomposes into
#       four separate mechanisms here rather than being ported: its navigation
#       fields become the browser's own route history, its identity fields become
#       claims in the signed token, its selection fields become path parameters,
#       and its re-entry discriminator (CDEMO-PGM-CONTEXT with the two condition
#       names at lines 29 to 31) disappears entirely, because an operation that
#       returns a field-error array has no first-entry-versus-re-entry distinction
#       left to make.
# WHY : Refactoring Rationale: the identity move is a correction and not merely a
#       relocation. CDEMO-USER-TYPE at lines 26 to 28 lived in storage the client
#       echoed back on the next request, so a client could in principle return a
#       type it was never issued; the group claim that replaces it is signed by the
#       user pool and the client cannot assert anything at all.
# WHY : Assumptions: control transfer between screens is a client concern. The
#       sign-on program's branch to one of two menu programs -- XCTL to 'COADM01C'
#       at app/cbl/COSGN00C.cbl lines 231 to 234 and to 'COMEN01C' at lines 236 to
#       239 -- becomes a route change the browser performs after reading the group
#       claim out of the token it just received, so no operation here returns a
#       next-screen instruction.
# WHY : Trade-offs: every operation below states its own security requirement even
#       though the document-level default already supplies the same value to the
#       five authenticated ones, so the requirement is written twice for those. The
#       duplication is accepted because the two protect against opposite mistakes:
#       the document default covers an operation added later whose author forgets a
#       security block, while the explicit per-operation declaration survives an
#       edit to that default. Neither alone is enough, and an operation's
#       authentication posture is the last thing in this contract that should be
#       inferable only from a value declared elsewhere.
paths:

  /api/v1/auth/signon:
    description: >-
      The credential exchange. It is one of the three paths in this document
      reachable without a bearer token, because it is the path that mints the
      first one, and the edge publishes it as an unauthenticated route.

    post:
      tags: [Sign-On]
      operationId: signOn
      summary: Exchange a user identifier and password for access tokens.
      description: >-
        Migrated from app/cbl/COSGN00C.cbl. Submits the credential to the
        configured Cognito user pool and returns the token set on success. It
        requires no bearer token, because it is the operation that mints the first
        one; it is published at the edge as the unauthenticated route
        POST /api/v1/auth/signon.


        Two behavioural divergences from the baseline are documented rather than
        hidden, and each is registered by identifier in
        docs/architecture/cobol-to-service-traceability.md section 7.4.


        D-SIGNON-CASE-SENSITIVE-PASSWORD: comparison is case-sensitive on the
        password. The baseline pushed both submitted fields through
        FUNCTION UPPER-CASE before it read the file and before it compared
        (app/cbl/COSGN00C.cbl lines 132 to 136, where the password fold's target
        WS-USER-PWD sits on line 136), which made it case-insensitive in both
        fields; the identity provider that now performs the comparison is
        case-sensitive on the password, and folding the password to restore the
        old behaviour would weaken the credential rather than preserve a feature.
        The identifier keeps its fold, because that one normalises a key rather
        than weakening a credential.


        D-SIGNON-EXISTENCE-UNIFORM: an unknown user and a wrong password are not
        distinguishable, and cannot be made so. Both return the 401 below
        carrying "Wrong Password. Try again ...". The baseline told them apart,
        and that distinction is given up deliberately because the pool fixes
        PreventUserExistenceErrors to ENABLED and the module publishes no input
        that could relax it.
      security: []
      x-required-authority: none
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The two credential fields of the sign-on screen.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignOnRequest"
      responses:
        "200":
          # Assumptions: this status carries TWO shapes, and ONE would not do. The
          #   pool answers an initial authentication with either a token set or a
          #   challenge, and a document declaring only the token set leaves the
          #   challenge nowhere to go but 500 -- which would make the first sign-on
          #   of every seeded user, whose password is temporary by construction,
          #   report a server fault. Both outcomes are 200 because in both the
          #   credential was ACCEPTED: the
          #   difference is whether tokens were issued, not whether the caller was
          #   recognised, and 401 would tell a caller its credential was wrong when
          #   it was right, while 500 would report a routine flow as a server
          #   fault.
          # Assumptions: the two are discriminated by a required outcome member
          #   rather than by which members are present, so a client branches on a
          #   value it can read rather than on an absence it has to infer.
          description: >-
            The credential was accepted. The body carries either the tokens the
            pool minted, with outcome AUTHENTICATED, or a challenge the caller must
            answer before tokens will be issued, with outcome CHALLENGE. On the
            first, the caller sends the access token as a bearer token on every
            other operation in this document; on the second, it calls the challenge
            operation.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/SignOnResponse"
                  - $ref: "#/components/schemas/SignOnChallenge"
                discriminator:
                  propertyName: outcome
                  mapping:
                    AUTHENTICATED: "#/components/schemas/SignOnResponse"
                    CHALLENGE: "#/components/schemas/SignOnChallenge"
        "400":
          # Assumptions: this status covers the two presence checks the baseline
          #   performs before it reads the file, at app/cbl/COSGN00C.cbl lines
          #   118 to 127, where a field that is SPACES or LOW-VALUES produces its
          #   own message and homes the cursor to that field. Both are structural
          #   input faults rather than credential rejections, which is why they
          #   are separated from the 401 below: a caller can correct a 400 from
          #   the response alone, whereas a 401 tells it only that the pair was
          #   refused.
          # Assumptions: the AGGREGATE message is the EARLIEST failing field's,
          #   not an aggregate sentence and not whichever violation the validation
          #   provider happened to report first. The baseline's two presence checks
          #   are one EVALUATE TRUE at app/cbl/COSGN00C.cbl lines 117 to 130 whose
          #   first matching branch sends the screen and stops, so a caller
          #   submitting both fields blank sees the identifier's message and never
          #   the credential's -- which is observable. SignOnRequest declares that
          #   order to the shared error advice, which sorts the entries into it and
          #   latches the first entry's own message as the top-level message.
          # Trade-offs: the array still carries EVERY offending field even though
          #   the baseline displays only one message, which is a deliberate
          #   addition rather than a divergence in the displayed text. Migration
          #   rule T7 makes the array the way a rejection is expressed, so a form
          #   can mark every failing control; the single sentence the baseline
          #   would have shown is preserved as the aggregate above it.
          description: >-
            A submitted field was absent, blank or over-length. The fieldErrors
            array carries one entry per offending field, keyed userId or password
            and ordered as the baseline checks them -- userId first. Each presence
            entry's message is the corresponding baseline literal:
            "Please enter User ID ..." (app/cbl/COSGN00C.cbl line 120) or
            "Please enter Password ..." (line 125). Both retain the space before
            the ellipsis exactly as the program writes it. The top-level message
            repeats the FIRST entry's message, reproducing the single sentence the
            baseline screen displays.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          # Trade-offs: one status carries what the baseline expressed as two
          #   outcomes, and the merge is a deliberate loss. The baseline reached
          #   "Wrong Password. Try again ..." only after a successful read whose
          #   password comparison failed (app/cbl/COSGN00C.cbl line 223) and
          #   reached "User not found. Try again ..." on the distinct
          #   record-not-found response at line 249, so an unauthenticated caller
          #   could learn whether a user identifier existed. What is bought by
          #   merging them is that user enumeration through this endpoint stops;
          #   what is given up is a diagnostic the baseline showed the operator.
          #   The divergence is registered as D-SIGNON-EXISTENCE-UNIFORM.
          # Assumptions: no provider posture makes the second literal reachable.
          #   The app client fixes PreventUserExistenceErrors to ENABLED at line
          #   609 of infra/modules/cognito/main.tf, and lines 603 to 608 of that
          #   file record the deliberate refusal to expose it as a module input,
          #   because a selectable legacy value would put the enumeration channel
          #   one tfvars line away. Advertising a posture no operator can select
          #   would state something untrue about the system.
          # Assumptions: the traceability link to line 249 is kept even though the
          #   literal is not a response of this operation. It is cited by the
          #   register entry above and carried in the browser application's
          #   message catalogue, so the lineage stays discoverable.
          description: >-
            The credential was refused. The message is
            "Wrong Password. Try again ..." (app/cbl/COSGN00C.cbl lines 242 to
            243, where the literal continues onto line 243) for both an unknown
            user identifier and an incorrect password, because the pool returns
            uniform user-existence errors. It is the only message this status
            carries: the baseline's "User not found. Try again ..." (line 249) is
            not reachable through this operation, and the divergence is
            registered as D-SIGNON-EXISTENCE-UNIFORM. No fieldErrors entry
            accompanies the message, because attributing the failure to one of
            the two submitted fields is the disclosure this status exists to
            withhold.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          # Assumptions: the baseline's WHEN OTHER arm at app/cbl/COSGN00C.cbl
          #   line 254 handles every file response that is neither success nor
          #   not-found, so it is the arm an infrastructure fault arrives on
          #   rather than a credential fault. Its migrated counterpart is any
          #   provider or integration failure -- the pool being unreachable, the
          #   request signature being rejected, or a challenge OTHER than
          #   NEW_PASSWORD_REQUIRED, which this flow genuinely cannot complete --
          #   and it deliberately does not reuse the 401 message, because telling a
          #   caller its password was wrong when the pool was simply unreachable
          #   would send it to reset a working credential.
          # Assumptions: NEW_PASSWORD_REQUIRED does NOT arrive here, and the
          #   exclusion is deliberate rather than an omission. Reporting it as this
          #   status would make the first sign-on of every seeded user a server
          #   fault and leave no path to a permanent password; it belongs to the 200
          #   above, carrying the challenge shape, because the credential was in fact
          #   accepted.
          description: >-
            The credential could not be evaluated. The message is
            "Unable to verify the User ..." (app/cbl/COSGN00C.cbl line 254). The
            body carries a correlation identifier and, where the failure was an
            abend, the externally safe abend aggregate described under
            AbendDetail; it never carries a provider diagnostic.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /api/v1/auth/refresh:
    description: >-
      Renewal of an expiring token set from the refresh token sign-on issued. It is
      pre-token by nature: the access token it renews may already have expired.

    post:
      tags: [Sign-On]
      operationId: refreshTokens
      summary: Exchange a refresh token for a new token set.
      description: >-
        Submits the identifier and the refresh token a previous sign-on returned,
        and returns a new token set. It exists so that a session outlives one access
        token lifetime without the user re-entering a credential; the baseline had
        no counterpart, because a CICS sign-on lasted as long as the terminal
        session did.


        It carries no bearer token because the token it would carry is the one being
        renewed, and a caller whose access token has already expired must still be
        able to renew.
      security: []
      x-required-authority: none
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The identifier the tokens were issued for and the refresh token to renew
          them with.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenRefreshRequest"
      responses:
        "200":
          # Assumptions: the renewed set carries a NEW refreshToken and the caller must
          #   replace the one it sent. The app client is provisioned with refresh-token
          #   rotation enabled and a zero retry grace period
          #   (infra/modules/cognito/main.tf:595-598), so the pool issues a replacement
          #   and invalidates the submitted token at once.
          # Assumptions: the property stays NULLABLE on the shared token-set shape even
          #   so, because the sign-on direction may legitimately return none, and the
          #   service passes through whatever the pool supplied rather than substituting
          #   a placeholder a caller could not use. A caller that kept the token it sent
          #   would have its SECOND renewal refused as a reuse, so the operation would
          #   appear to work once and then fail permanently.
          description: >-
            The refresh token was accepted. The body is the same token set shape
            sign-on returns, carrying a new access token, a new identity token and a
            new refreshToken that replaces the one submitted.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignOnResponse"
        "400":
          description: >-
            A submitted field was absent or blank. The fieldErrors array carries one
            entry per offending field, keyed userId or refreshToken.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: >-
            The refresh token was expired, revoked, or not issued to the identifier
            supplied. The caller signs on again; no distinction between the three is
            reported, for the same non-disclosure reason sign-on gives.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          description: >-
            The renewal could not be evaluated. The message is
            "Unable to verify the User ..." (app/cbl/COSGN00C.cbl line 254) and the
            body never carries a provider diagnostic.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"


  # Assumptions: this path is the one the edge already publishes. It is the second
  #   of the four deliberately unauthenticated route keys in
  #   infra/modules/api-gateway-http/variables.tf, so the gateway forwards it with
  #   no authorizer attached, and it must be unauthenticated for the same reason
  #   sign-on is: a caller answering a challenge has no token yet, that being what
  #   the exchange exists to obtain.
  /api/v1/auth/challenge:

    post:
      tags: [Sign-On]
      operationId: answerSignOnChallenge
      summary: Set a permanent password in answer to a sign-on challenge.
      description: >-
        Completes a sign-on that returned outcome CHALLENGE with challengeName
        NEW_PASSWORD_REQUIRED, by setting the permanent password the pool is asking
        for and returning the token set the sign-on could not issue.


        This operation has no baseline counterpart, and that is stated rather than
        papered over: the reference sign-on compared a stored eight-character
        password directly at app/cbl/COSGN00C.cbl lines 211 to 256 and had no
        notion of a credential that must be changed before use, so no message
        literal is carried across here and none of the three sign-on sentences is
        reused. It exists because the credential moved to a managed user pool whose
        seeded users are provisioned with temporary passwords, which makes this
        exchange the FIRST thing every provisioned user does. The divergence is
        registered as D-PASSWORD-CHALLENGE in
        docs/architecture/cobol-to-service-traceability.md, and it follows from the
        entry D-4 there, which records the plaintext credential field not being
        carried forward.


        It is idempotent in neither direction and must not be retried blindly: the
        session value is single-use, so a repeat with the same session is refused
        and a fresh sign-on is required.
      security: []
      x-required-authority: none
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The session value from the challenge body, the identifier it was raised
          for, and the permanent password to set.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignOnChallengeRequest"
      responses:
        "200":
          # Assumptions: this returns the AUTHENTICATED shape only, never the
          #   challenge shape, so it cannot loop. The pool issues tokens once the
          #   new password is accepted, and a second challenge would mean a
          #   configuration this contract does not publish -- reported as 500 below
          #   rather than returned as another challenge a client would have no
          #   defined way to answer.
          description: >-
            The password was set and the pool issued tokens. The body is the same
            shape a challenge-free sign-on returns, carrying outcome AUTHENTICATED.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SignOnResponse"
        "400":
          description: >-
            A submitted field was absent, or the proposed password was refused by
            the pool's password policy. The fieldErrors array names the offending
            field -- userId, session or newPassword -- and for a policy refusal the
            entry carries the pool's own reason rather than a restatement of the
            policy, because the policy is configured in the pool and this contract
            does not hold a second copy of it.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          # Trade-offs: an expired, altered, already-used or mismatched session all
          #   reach this one status with one message. Distinguishing them would tell
          #   an unauthenticated caller which of the four it had, and the remedy is
          #   the same in every case: sign on again.
          description: >-
            The session could not be accepted -- it has expired, has already been
            used, was altered, or was not issued for the identifier supplied. The
            body names no reason beyond that, and the remedy is to sign on again.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          description: >-
            The exchange could not be evaluated -- the pool was unreachable, the
            request signature was rejected, or the pool raised a further challenge
            this flow does not publish a contract for. The body carries a
            correlation identifier and never a provider diagnostic.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"


  # Assumptions: this path is the FOURTH deliberately unauthenticated route key in
  #   infra/modules/api-gateway-http/variables.tf, and the only one of the four that
  #   sits AFTER a token has stopped being wanted rather than before one exists. It is
  #   unauthenticated for a reason that reads backwards until it is stated: this is the
  #   operation whose purpose is to make a credential stop working, so gating it behind
  #   a valid access token would refuse it in exactly the case it matters -- a session
  #   abandoned rather than closed, whose access token has expired while its refresh
  #   token has twenty-nine days left. Authority is possession of the refresh token,
  #   which the pool verifies; a caller that cannot produce it revokes nothing.
  /api/v1/auth/signout:
    description: >-
      Revocation at the identity pool of the refresh token a session renews from. It
      has no baseline counterpart: signing off under the transaction monitor
      transferred control back to the sign-on screen and revoked nothing, because a
      terminal session was the session and it lasted until the terminal disconnected.

    post:
      tags: [Sign-On]
      operationId: signOut
      summary: Revoke the refresh token a session renews from.
      description: >-
        Asks the pool to revoke the submitted refresh token, so that no further token
        set can be minted from it. This is what makes signing out an event at the pool
        rather than a change of state in one browser: the refresh token is provisioned
        with a thirty-day life, so a copy taken from a browser store, a synchronised
        profile or a shared workstation would otherwise keep minting access tokens for
        the rest of that month.


        It carries no bearer token, and no identifier. Authority is possession of the
        refresh token itself, which is also all the provider's revocation call accepts
        -- it takes the token and this service's own client credentials and no user
        name. Requiring a valid access token instead would refuse the revocation
        precisely when the session most needs ending.


        It is idempotent. A token the pool will not accept -- expired, already
        revoked, already rotated away, or not a refresh token at all -- answers 204
        exactly as a revoked one does, because an unusable token is the outcome the
        caller asked for and reporting a failure would both misstate that and hand an
        unauthenticated caller a test for whether a token is live. RFC 7009 section
        2.2 takes the same position for the same reason.


        Revocation stops RENEWAL. It does not invalidate an access token already
        issued: every service validates a bearer by signature, issuer and expiry
        against the pool's published keys, which consults no revocation state, so a
        bearer already in a caller's hands stays acceptable until it expires --
        bounded by access_token_validity_minutes, 60 by default. A client must
        therefore discard its own copies as well as calling this operation, and it
        must do so even when this operation fails.
      security: []
      x-required-authority: none
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The refresh token to revoke.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignOutRequest"
      responses:
        # Assumptions: 204 rather than 200 with a body, because there is nothing this
        #   operation could truthfully put in one. It reports neither whether the token
        #   was live nor whose it was, so a body would publish a shape carrying only
        #   restatements of the request.
        "204":
          description: >-
            The token will mint nothing further. This is the answer both for a token
            the pool revoked and for one it would not accept, and the two are
            deliberately indistinguishable.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
        "400":
          description: >-
            The submitted token was absent, blank or above its declared maximum. The
            fieldErrors array carries one entry keyed refreshToken.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        # Assumptions: no 401 is declared, and its absence is the point of the 204
        #   above rather than an oversight. There is no credential this operation can
        #   refuse: the token it is given is the thing it exists to invalidate, so a
        #   token it cannot use has already produced the caller's outcome.
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          description: >-
            The pool could not be reached or answered a fault, so the token may still
            be live and able to mint. The message is
            "Unable to verify the User ..." (app/cbl/COSGN00C.cbl line 254), which is
            this tag's declared sentence for an exchange that could not be evaluated;
            the body never carries a provider diagnostic. A client clears its own
            state regardless, and this status is what tells the operator the token was
            not revoked.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"


  /api/v1/auth/users:

    get:
      tags: [User Administration]
      operationId: listUsers
      summary: List users one bounded page at a time, positioned by key.
      description: >-
        Migrated from app/cbl/COUSR00C.cbl, whose browse filled a ten-row screen
        and reported whether another row followed. Requires the carddemo-admin
        authority. A page holds at most ten rows and that size is set by the
        contract rather than chosen by the caller. Paging is by key in one
        direction at a time: send no cursor for the first page, then send the
        lastKey of the page you hold to move forward or its firstKey to move
        back, with direction naming which of the two you sent.


        Reaching an end of the file is a success, never a failure. A request that
        lands on the last page returns 200 with hasNext false, and a request that
        cannot move at all -- because the caller asked to go back from the first
        page or forward from the last -- returns 200 with the boundary page
        unchanged and the cursor for the direction travelled absent, or hasNext
        false when that direction was forward. The baseline
        distinguished these two situations by message and this contract keeps
        them distinguishable by response: an arrival moved and reports a boundary,
        whereas a refused move returns the page the caller already had.
      security:
        - bearerAuth: []
      x-required-authority: carddemo-admin
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
        # Alternatives Considered: positioning a page by an ordinal or a row
        #   offset, which is the obvious shape and is rejected on a specific
        #   defect. Under concurrent insertion the number of rows preceding a
        #   position changes between one request and the next, so an offset-paged
        #   reader silently skips some rows and shows others twice, while a key
        #   already read keeps its place in the ordering no matter what is
        #   inserted around it. The baseline's own browse was already a cursor
        #   over keys, so this is a one-for-one substitution rather than an
        #   approximation: it stored the page's last key and first key as real key
        #   values (app/cbl/COUSR00C.cbl lines 435 and 389) and held no count of
        #   rows consumed anywhere.
        # Assumptions: both cursors are EXCLUSIVE -- the row they name is not
        #   returned again. The baseline established this with an extra read
        #   issued before each page was filled: one further READNEXT at line 289,
        #   guarded by "IF EIBAID NOT = DFHENTER AND DFHPF7 AND DFHPF3" on line
        #   288, and the mirrored READPREV at line 343 guarded by only two keys on
        #   line 342. Each steps past the cursor record itself, so a page begins
        #   at the row after the one the caller named.
        # Assumptions: a cursor is an opaque sealed token minted by this service,
        #   not a user identifier. It carries the key inside an authenticated
        #   payload bound to the query and the subject it was issued to, so a
        #   caller cannot mint one naming a row it was never shown, and it expires.
        #   Its shape is a version marker, a payload and a code separated by
        #   periods; treat it as text to be echoed unchanged and never parse or
        #   construct one.
        # Alternatives Considered: TWO cursor parameters, named lastKey and firstKey
        #   after the two members a caller copies them FROM. Rejected -- two inputs
        #   named after the two members invite a client to send the wrong one for its
        #   direction, because nothing in either name says which direction it belongs
        #   to. One input named cursor cannot be the wrong one, the direction
        #   parameter beside it carrying that choice explicitly instead.
        # Assumptions: the envelope publishes exactly four members -- items,
        #   firstKey, lastKey and hasNext -- so a caller pages from firstKey and
        #   lastKey, and no separately named nextCursor or prevCursor member exists
        #   to copy. Refactoring Rationale: those two names were WITHDRAWN, as the
        #   card contract records at its own envelope, so a reader meeting them in
        #   history should not restore them. Keeping the four also makes this document
        #   agree with the card and transaction contracts, which declare the same
        #   single parameter, so there is one paging shape across this migration
        #   rather than three.
        # Assumptions: the direction is carried in the SEAL of each token as well
        #   as in the direction parameter, so replaying the previous-page position
        #   with direction next cannot silently answer with the wrong page: the
        #   token fails to open and the request is refused with 400. That is what
        #   makes a single parameter as safe as two named ones while removing the
        #   both-supplied and mismatched-pair cases two parameters had to define
        #   errors for.
        - $ref: "#/components/parameters/Cursor"
        # Alternatives Considered: accepting a page number, which the baseline
        #   screen genuinely had and which this contract still refuses. The field
        #   exists as PAGENUMI PIC X(8) at app/cpy-bms/COUSR00.CPY line 60 and as
        #   CDEMO-CU00-PAGE-NUM PIC 9(08) at app/cbl/COUSR00C.cbl line 70, but it
        #   is display only: it is moved to the screen at lines 327 and 376 and is
        #   never used to position a read. Its single control-flow use is the
        #   guard at line 248 that decides whether the "already at the top"
        #   message is shown. Accepting it as a parameter would therefore invent
        #   positioning semantics the baseline never had, and an absolute ordinal
        #   cannot be honoured by a key-positioned query without enumerating every
        #   row ahead of the cursor -- which is the enumeration this design exists
        #   to avoid.
        - $ref: "#/components/parameters/PagingDirection"
      responses:
        "200":
          # Assumptions: a cursor naming a row since deleted is NOT a not-found
          #   condition. The baseline's read on the supplied key keys on
          #   DFHRESP(NOTFND) at app/cbl/COUSR00C.cbl line 600 and falls through
          #   to a CONTINUE, treating the position as the start of the file rather
          #   than as an error, so a caller holding a cursor to a since-deleted
          #   user is repositioned instead of being refused. A cursor that is
          #   malformed, altered or expired is a different matter and is refused
          #   with 400, because that one cannot be opened at all.
          # Assumptions: reaching or failing to leave an end of the file is a 200
          #   in every case, never a 4xx, and the baseline's five boundary
          #   messages fall into two classes that must not be merged. A GUARD is a
          #   move that never happened, because the caller was already at that end:
          #   "You are already at the top of the page..." (line 251, gated by the
          #   page-number test on line 248) and
          #   "You are already at the bottom of the page..." (line 273, the longest
          #   string in this context at 44 characters). An ARRIVAL is a move that
          #   did happen and landed on an end:
          #   "You are at the top of the page..." (line 603),
          #   "You have reached the bottom of the page..." (line 637) and
          #   "You have reached the top of the page..." (line 671). All five are in
          #   app/cbl/COUSR00C.cbl and none carries a space before its ellipsis.
          #   The two classes stay distinguishable in the response rather than by
          #   message: a guard returns the same page the caller already held, while
          #   an arrival returns a different page that does not offer the
          #   direction travelled -- hasNext false going forward, or no firstKey
          #   going back.
          # Alternatives Considered: returning these five as the response message
          #   so the browser could display them without deciding which applies.
          #   Rejected because a caller can already tell the five apart from the
          #   direction it asked for, the indicator it held before the request, and
          #   the indicator it holds after -- and because putting a sentence in a
          #   SUCCESS body would make every ordinary page carry a message field
          #   that is usually empty. The browser's own message catalog holds the
          #   five literals, which is where every user-visible string in the
          #   migrated system lives.
          description: >-
            One page of at most ten rows in ascending user-identifier order,
            together with the two boundary cursors for continuing in either
            direction and the forward availability indicator. An exhausted or fully
            filtered page carries an empty items array rather than an error.


            This status also covers both boundary situations the baseline reported
            by message. A move that could not happen because the caller was
            already at that end returns the page it already held -- the baseline's
            "You are already at the top of the page..." and
            "You are already at the bottom of the page...". A move that happened
            and landed on an end returns the new page, which does not offer that
            direction -- the baseline's
            "You are at the top of the page...",
            "You have reached the bottom of the page..." and
            "You have reached the top of the page...". Neither situation is an
            error, and none of these five strings carries a space before its
            ellipsis.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageResponse"
        "400":
          description: >-
            A supplied cursor could not be opened -- it was malformed, altered,
            expired, or presented for a different query or subject than it was
            issued for -- or both cursors were supplied at once, or direction did
            not name one of the two permitted values. The fieldErrors array
            identifies the offending parameter by name.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "500":
          # Assumptions: the baseline reaches this arm from three separate places
          #   -- the WHEN OTHER of each of its three reads, at
          #   app/cbl/COUSR00C.cbl lines 610, 644 and 678 -- and writes the same
          #   literal at all three, so one status with one message is the faithful
          #   mapping rather than a flattening of distinct outcomes.
          description: >-
            The user file could not be read. The message is
            "Unable to lookup User..." (app/cbl/COUSR00C.cbl lines 610, 644 and
            678), with no space before the ellipsis, as the program writes it.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

    post:
      tags: [User Administration]
      operationId: createUser
      summary: Create one user row.
      description: >-
        Migrated from app/cbl/COUSR01C.cbl. Requires the carddemo-admin
        authority. The body carries the four values the create screen validates;
        it carries no password, because this service creates no credential, and it
        carries no Cognito subject, because this service creates the pool account
        itself and reads the subject back from the provider -- see the
        create-request schema for the full rationale on both. On success the
        response is 201 with the created row, including the subject the row was
        bound to and the ONE-TIME credential the new account was created with, and
        a Location header naming the row.


        Refactoring Rationale: that credential is new to this response, and its
        absence was a defect rather than a hardening measure. The pool account is
        created with message delivery suppressed -- the pool declares no email or
        phone attribute, so there is no address to deliver to -- and previously with
        no credential supplied either, so the provider minted one internally and
        sent it nowhere. A user created here held an account, the right group and a
        row bound to its subject, and could never sign on, because the credential
        was known to nobody and no operation existed to reset it. The value is now
        generated by the service, returned once, and is not retrievable afterwards:
        no column stores it and no other operation answers with it. It is
        single-use in the strict sense -- the account stands in the provider's
        force-change state, so presenting it raises the new-password challenge that
        POST /api/v1/auth/challenge answers and nothing else can be done with it.


        The success message the baseline composed --
        "User " followed by the identifier and then " has been added ..."
        (app/cbl/COUSR01C.cbl lines 255 to 258, the literal on line 257) -- is not
        returned as a field. The browser client holds every user-visible string in
        its own catalog and composes that sentence from the identifier it receives,
        which is where the two thank-you strings and the screen titles also live;
        a server-rendered sentence would be a second source for one string and the
        two could disagree. Failure messages ARE returned, because which branch
        failed is knowledge only this service has.
      security:
        - bearerAuth: []
      x-required-authority: carddemo-admin
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The new row's values, in the order the create screen validates them.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: >-
            The row was created. The body is the stored row as a subsequent read
            would return it, PLUS credentialSecretName -- the NAME of the managed-secret
            entry holding the one-time credential for its owner's first sign-on. The
            Location header addresses the row.


            NO CREDENTIAL IS IN THIS BODY. Onboarding is a two-step act: read this
            locator, then retrieve the value from that entry with
            secretsmanager:GetSecretValue and the customer-managed key's grant, and hand
            THAT over. The retrieval is privileged and audited, which is the point of it;
            a browser session cannot perform it and a task role can.


            Refactoring Rationale: this description asserted that the credential itself
            was in the body and appeared nowhere else. Both halves were wrong and the
            schema below, the response record and the later prose on
            CreatedUserResponse all already said so -- the value is published to a
            per-user managed-secret entry, so the response was never its only home, and
            what crosses the wire is a name. The claim is corrected rather than softened
            because an operator acting on it looked for a credential property that does
            not exist, concluded the create had failed to produce one, and had no reason
            to look in the secret store where it actually was.
          headers:
            Location:
              description: >-
                Absolute path of the created row, of the form
                /api/v1/auth/users/{userId}, so a caller need not compose the
                address itself.
              required: true
              schema:
                type: string
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatedUserResponse"
        "400":
          # Assumptions: the order of entries in fieldErrors is the order the
          #   baseline validated in, because the first error a user sees is
          #   observable behaviour. Its cascade short-circuits in physical screen
          #   order at app/cbl/COUSR01C.cbl lines 120, 126, 132 and 144 -- first
          #   name, last name, user identifier, then user type -- and the
          #   create-request schema's property order reproduces exactly that
          #   sequence.
          # Assumptions: the key list below names FOUR fields and no more, matching
          #   the four baseline literals and the four properties the create-request
          #   schema carries. Naming a fifth -- cognitoSub, for instance, which this
          #   request does not accept -- would promise a fieldErrors entry that
          #   cannot be produced: a body carrying an unknown member is refused by
          #   additionalProperties: false, which is a schema violation rather than a
          #   field-level validation failure and so reports no logical field at
          #   all.
          # Assumptions: an ABSENT value and an UNUSABLE one are described separately
          #   below, because only the first has a baseline literal. The four
          #   emptiness sentences are transcribed from the program; the width and
          #   domain sentences are authored, because a 3270 field cannot hold a
          #   character past its declared width and the program therefore has no
          #   over-length branch to take a literal from.
          # Assumptions: the userId width sentence is reachable in TWO ways and the
          #   description does not distinguish them, because a caller cannot act on
          #   the difference. The request schema's own maxLength refuses a submitted
          #   value longer than eight, and the service additionally refuses one whose
          #   FOLDED form is longer -- case folding is not length-preserving, so
          #   eight characters of a letter that upper-cases to two exceed the stored
          #   key's width while satisfying maxLength. Both mean the same thing to the
          #   caller: the identifier this operation can store is at most eight
          #   characters.
          # Assumptions: a userId whose folded form is EMPTY carries the emptiness
          #   sentence rather than the width one, because that is what it is. A value
          #   of whitespace the baseline's own emptiness test reads as present -- a
          #   tab equals neither SPACES nor LOW-VALUES -- still leaves nothing once
          #   trimmed.
          description: >-
            A required value was absent, or a supplied value could not be used.
            Each fieldErrors entry is keyed by the logical field -- firstName,
            lastName, userId or userType. An ABSENT value carries the baseline
            literal for that field: "First Name can NOT be empty..."
            (app/cbl/COUSR01C.cbl line 120),
            "Last Name can NOT be empty..." (line 126),
            "User ID can NOT be empty..." (line 132) or
            "User Type can NOT be empty..." (line 144). None of these four
            carries a space before its ellipsis. A value that is present but
            unusable carries the authored sentence for that field instead:
            "First Name must be at most 20 characters...",
            "Last Name must be at most 20 characters...",
            "User ID must be at most 8 characters..." -- which also answers an
            identifier whose upper-cased form exceeds that width, since the stored
            key holds the folded value -- "User Type must be exactly one
            character..." or "User Type must be A or U...".
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "409":
          # Assumptions: the baseline folds two distinct file responses into one
          #   handler -- WHEN DFHRESP(DUPKEY) at app/cbl/COUSR01C.cbl line 260 and
          #   WHEN DFHRESP(DUPREC) at line 261 fall through together -- so one
          #   status with one message is what the baseline actually produced, not
          #   a simplification of it. The condition is now enforced by the primary
          #   key on auth.users, so the outcome cannot be lost to a race between
          #   a check and an insert.
          # Trade-offs: the message is reproduced with its grammatical error
          #   intact -- "User ID already exist...", missing the s, at line 263.
          #   Correcting it would silently change a string a client may match on
          #   or a user may recognise, and an externally observable string is part
          #   of the interface whether or not it reads well. The cost is that this
          #   contract publishes a visible mistake; the gain is that the migration
          #   changes no observable output it was not asked to change.
          description: >-
            A row with the submitted user identifier already exists. The message
            is "User ID already exist..." (app/cbl/COUSR01C.cbl line 263),
            reproduced exactly as the program writes it, including the missing s
            and with no space before the ellipsis.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          description: >-
            The row could not be written. The message is
            "Unable to Add User..." (app/cbl/COUSR01C.cbl line 270), with no
            space before the ellipsis.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"


  /api/v1/auth/users/{userId}:
    description: >-
      One user row addressed by its eight-character identifier: read, replaced or
      deleted. Every operation on it requires the carddemo-admin authority.

    # Assumptions: the selected user travels in the path on all three operations
    #   below, which is what replaces the baseline's input field on the screen
    #   (USRIDINI PIC X(8) at app/cpy-bms/COUSR02.CPY line 60 and
    #   app/cpy-bms/COUSR03.CPY line 60) and the selection column on the list
    #   screen. Putting it in the path makes every request self-describing and
    #   therefore independently authorisable, which a value carried in a
    #   round-tripped buffer was not.
    parameters:
      - $ref: "#/components/parameters/UserIdPath"

    get:
      tags: [User Administration]
      operationId: getUser
      summary: Read one user row.
      description: >-
        Migrated from the record-load path shared by app/cbl/COUSR02C.cbl and
        app/cbl/COUSR03C.cbl, and it serves both their screens. Requires the
        carddemo-admin authority. In the baseline this load was
        PROCESS-ENTER-KEY at app/cbl/COUSR02C.cbl line 143, which blanked the
        four editable fields (lines 157 to 164) and then refilled them from the
        record just read (lines 166 to 172); here it simply returns the row.


        One structural difference is deliberate. The delete screen's load read the
        record for update -- EXEC CICS READ carrying the UPDATE option, at
        app/cbl/COUSR03C.cbl lines 269 to 278 with UPDATE on line 275 -- and so
        held a lock while the operator decided. This operation takes no lock, because a stateless
        request cannot hold one across a user's think-time and a lock held that
        way is the failure mode the design removes. Nothing is lost by it: the
        sibling migration db/migration/V1__auth.sql records why no version column
        or conflict outcome is needed, namely that the baseline never carried a
        before-image across a screen turn on this file.
      security:
        - bearerAuth: []
      x-required-authority: carddemo-admin
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      responses:
        "200":
          description: >-
            The stored row. Identical in shape to the create and update responses,
            so one client type covers all three.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "400":
          description: >-
            The path segment was blank, was wider than eight characters, or
            carried a character the stored key cannot be drawn from. A blank segment
            carries "User ID can NOT be empty..." (app/cbl/COUSR02C.cbl lines 148
            and 182, and app/cbl/COUSR03C.cbl lines 147 and 179), with no space
            before the ellipsis. An over-wide one carries
            "User ID must be at most 8 characters...", a sentence this service
            authors rather than transcribes, because an eight-position screen field
            could not submit a ninth character for the baseline to refuse. A segment
            carrying a character outside the printable single-byte range PIC X(08)
            means carries "User ID contains an unsupported character..." -- which is
            also what refuses a segment that would have folded WIDER than eight
            positions, since only a character from outside that range can change a
            value's width when it is upper-cased. Each of these is keyed userId in
            the fieldErrors array, and none is reported as a 404 instead: a value
            the column could not hold is not a key any row could carry.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            No row carries the requested identifier. The message is
            "User ID NOT found..." (app/cbl/COUSR02C.cbl line 342 and
            app/cbl/COUSR03C.cbl line 289), with no space before the ellipsis.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "500":
          description: >-
            The row could not be read. The message is
            "Unable to lookup User..." (app/cbl/COUSR02C.cbl line 349 and
            app/cbl/COUSR03C.cbl line 296).
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

    put:
      tags: [User Administration]
      operationId: updateUser
      summary: Update the mutable values of one user row.
      description: >-
        Migrated from app/cbl/COUSR02C.cbl, whose update arm is the
        UPDATE-USER-INFO paragraph at line 177, reached from the save key, while
        its load arm is PROCESS-ENTER-KEY at line 143. Requires the
        carddemo-admin authority. The path carries which row to change and the
        body carries only what may change, so the identifier appears once per
        request and cannot be contradicted between the two.


        A body that matches the stored row in every field is refused with 400.
        That is the baseline's behaviour, not an addition: it compared each
        submitted field against the record, and where nothing differed it took the
        else branch at app/cbl/COUSR02C.cbl line 237 and wrote
        "Please modify to update ..." in red at lines 239 to 241. The colour is
        what settles the status, because that same message field is written in
        three colours in this program -- red at line 241, neutral at line 338 and
        green at line 371 -- so red marks it as a rejection rather than as advice.


        The success message the baseline composed --
        "User " followed by the identifier and then " has been updated ..."
        (lines 372 to 375, the literal on line 374) -- is not returned as a field,
        for the reason given on the create operation.
      security:
        - bearerAuth: []
      x-required-authority: carddemo-admin
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
      requestBody:
        required: true
        description: >-
          The three values this operation may change, in the order the update
          screen validates them.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: >-
            The row as stored after the change.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserResponse"
        "400":
          # Assumptions: fieldErrors is ordered as the baseline validated, whose
          #   cascade for this screen runs at app/cbl/COUSR02C.cbl lines 188, 194
          #   and 206 once the identifier check at line 182 has become the path
          #   parameter and the password check at line 200 has gone with the
          #   credential. The update-request schema's property order is exactly
          #   those three survivors.
          # Assumptions: the baseline validated the identifier TWICE -- once on
          #   Enter at lines 146 to 151 and again on the save key at lines 180 to
          #   185 -- because each screen turn resubmitted the whole map and either
          #   arm could be reached first. One check suffices here, since the value
          #   arrives in the path and is validated before the operation runs at
          #   all.
          # Assumptions: the key naming an offending field is the logical field,
          #   derived from where the baseline homed the cursor. Those targets are
          #   per-branch and not uniform -- its catch-all arm homes to the first
          #   name field at lines 210 to 212 rather than to the field that
          #   actually failed -- so the key reports the field at fault rather than
          #   reproducing a cursor position a browser has no use for.
          description: >-
            A submitted value was absent or outside its domain, or every value
            matched the stored row. Each fieldErrors entry is keyed firstName,
            lastName or userType and carries that field's baseline literal:
            "First Name can NOT be empty..." (app/cbl/COUSR02C.cbl line 188),
            "Last Name can NOT be empty..." (line 194) or
            "User Type can NOT be empty..." (line 206). The unchanged-body case
            instead carries the aggregate message
            "Please modify to update ..." (line 239) with no field attribution,
            because no single field is at fault. Note the spacing difference
            between the two kinds: the three validation literals carry no space
            before the ellipsis and the aggregate one does, exactly as the program
            writes them.


            The path segment is refused on the same terms as it is on the read and
            delete operations, and before the body is examined: blank carries
            "User ID can NOT be empty..." (app/cbl/COUSR02C.cbl lines 148 and 182),
            wider than eight characters carries
            "User ID must be at most 8 characters...", and a character outside the
            printable single-byte range PIC X(08) means -- including one that would
            widen the derived key past eight positions when upper-cased -- carries
            "User ID contains an unsupported character...". All three are keyed
            userId, and in each case the stored row is neither read nor changed.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            No row carries the identifier in the path. The message is
            "User ID NOT found..." (app/cbl/COUSR02C.cbl lines 342 and 379).
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "500":
          description: >-
            The change could not be written. The message is
            "Unable to Update User..." (app/cbl/COUSR02C.cbl line 386).
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

    delete:
      tags: [User Administration]
      operationId: deleteUser
      summary: Delete one user row, with the deletion explicitly confirmed.
      description: >-
        Migrated from app/cbl/COUSR03C.cbl. Requires the carddemo-admin
        authority. The row is identified by the path and the deletion must be
        confirmed by the query parameter below; a request without that
        confirmation deletes nothing.


        The success message the baseline composed --
        "User " followed by the identifier and then " has been deleted ..."
        (app/cbl/COUSR03C.cbl lines 318 to 321, the literal on line 320) -- is not
        returned, both for the reason given on the create operation and because a
        204 carries no body at all.


        A 204 means the row is gone. Removal of the identity behind it is applied
        to the provider after this operation's transaction commits and is recorded
        durably until the provider agrees, so the deleted user may briefly still
        exist in the provider -- where they authenticate but hold no membership this
        service records and are refused at every route requiring one. That ordering
        is deliberate: the reverse, removing the identity first, is the direction no
        later operation repairs without provisioning the account again.
      security:
        - bearerAuth: []
      x-required-authority: carddemo-admin
      parameters:
        - $ref: "#/components/parameters/CorrelationIdHeader"
        # Refactoring Rationale: the baseline made deleting a two-step act. Its
        #   load arm read the record and then invited a second, different
        #   keystroke -- "Press PF5 key to delete this user ..." in neutral at
        #   app/cbl/COUSR03C.cbl lines 283 and 284 -- so displaying a user and
        #   destroying one were never the same operation. That safeguard cannot
        #   survive as a keystroke over HTTP, and dropping it would mean a
        #   prefetch, a retried request or a crawler following a link could
        #   destroy a row: navigation must never delete. Requiring an explicit
        #   affirmative parameter reconstructs the two-step act from the
        #   information a single request carries -- the caller must have decided
        #   to delete in order to have sent it.
        # Alternatives Considered: carrying the confirmation in a request body,
        #   which was rejected because a body on DELETE has no defined semantics
        #   and intermediaries are permitted to drop it, so the safeguard could
        #   vanish in transit and the deletion would proceed unconfirmed. A
        #   required query parameter cannot be silently discarded: if it is
        #   missing the request is refused.
        # Assumptions: the value must be true, not merely present. Accepting any
        #   value would let confirmed=false delete a row, which is the exact
        #   opposite of what the caller wrote.
        - name: confirmed
          in: query
          required: true
          description: >-
            Explicit confirmation that the row named in the path is to be
            destroyed. Must be the boolean true; absent or false is refused with
            400 and nothing is deleted.
          schema:
            type: boolean
            const: true
      responses:
        "204":
          # Alternatives Considered: returning 200 with the deleted row, which
          #   was rejected because it would hand back a representation of a row
          #   that has just ceased to exist and invite a client to cache or
          #   redisplay it. 204 states the outcome and nothing more.
          description: >-
            The row was deleted. No body, by design.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
        "400":
          description: >-
            The confirmation was absent or was not true, or the path segment was
            blank, wider than eight characters, or carried a character the stored key
            cannot be drawn from. In the confirmation case the fieldErrors array
            carries a single entry keyed confirmed and nothing is deleted; in the
            identifier case the entry is keyed userId and carries
            "User ID can NOT be empty..." (app/cbl/COUSR03C.cbl lines 147 and 179)
            when the segment is blank,
            "User ID must be at most 8 characters..." when it is over-wide, or
            "User ID contains an unsupported character..." when it carries a
            character outside the printable single-byte range PIC X(08) means --
            which includes any character that would widen the derived key when
            upper-cased. Nothing is deleted in any of them.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            No row carries the identifier in the path. The message is
            "User ID NOT found..." (app/cbl/COUSR03C.cbl lines 289 and 325).
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "405":
          $ref: "#/components/responses/MethodNotAllowed"
        "406":
          $ref: "#/components/responses/NotAcceptable"
        "500":
          # Refactoring Rationale: the message on this branch says Update on a
          #   delete failure. That is what the baseline writes -- the literal at
          #   app/cbl/COUSR03C.cbl line 332 is "Unable to Update User...", the
          #   same string this program's sibling writes at
          #   app/cbl/COUSR02C.cbl line 386 -- and it reads as a copy of the
          #   update program's handler that was never reworded for its new home.
          # Trade-offs: it is carried across as it stands rather than reworded to
          #   say Delete. Message fidelity was chosen over readability because the
          #   string is externally observable: a client matching on it, or an
          #   operator recognising it from the terminal, would both be broken by a
          #   change this migration was not asked to make. The cost is a message
          #   that names the wrong verb; the alternative was an undocumented
          #   divergence in observable output, which is worse. It is documented,
          #   not fixed.
          description: >-
            The row could not be deleted. The message is
            "Unable to Update User..." (app/cbl/COUSR03C.cbl line 332) -- the word
            Update, on the delete path, exactly as the program writes it.
          headers:
            X-Correlation-Id:
              $ref: "#/components/headers/XCorrelationId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"


components:

  # WHY : Assumptions: authority comes from the signed cognito:groups claim and
  #       from nothing else. The shared kernel's JwtRoleConverter reads that claim
  #       and grants carddemo-admin for the baseline's 'A' user type and
  #       carddemo-user for 'U', which are the two values app/cpy/COCOM01Y.cpy
  #       lines 26 to 28 enumerate, and the group names it recognises are the
  #       literals the Cognito module provisions. No request field, header or body
  #       value can add an authority.
  # WHY : Assumptions: all five user-administration operations require
  #       carddemo-admin, and the evidence is that every one of the five baseline
  #       programs returned to the ADMIN menu rather than the general one --
  #       'COADM01C' at app/cbl/COUSR00C.cbl line 126, app/cbl/COUSR01C.cbl line
  #       94, app/cbl/COUSR02C.cbl lines 114 and 125, and app/cbl/COUSR03C.cbl
  #       lines 113 and 124. A screen reachable only from the administrator's menu,
  #       and returning only to it, was administrator-only in effect, so
  #       restricting the whole family transcribes the baseline's reachability
  #       rather than imposing a new restriction.
  # WHY : Trade-offs: the scopes array is empty, which is a limitation of the
  #       scheme type rather than an absence of a scope requirement. A bearer
  #       scheme in OpenAPI cannot express scopes; the token the service accepts
  #       must still carry the pool's administrative sign-in scope, and both the
  #       edge route authorization and this service's own token validator assert
  #       it. The requirement is stated in the description below so it is not
  #       lost.
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >-
        Access token minted by the CardDemo Cognito user pool and returned by the
        sign-on operation, presented as an HTTP bearer credential. The service
        accepts only an access token, identified by a token_use claim of access:
        the same pool signs identity tokens with the same key, and an identity
        token describes a user to a client rather than authorising an API call,
        so accepting one would be a confusion of purpose. The token must also
        have been issued to this deployment's app client and must carry the
        pool's administrative sign-in scope; a Cognito access token carries no
        audience claim, so the client is checked through its client_id claim
        instead. Authorities are derived from the cognito:groups claim --
        carddemo-admin or carddemo-user -- and never from anything the caller
        supplies in the request.

  # WHY : Assumptions: the shared kernel's correlation filter names this header on
  #       both directions of every request and also places the value in the
  #       logging context, so a caller reporting a problem can quote one
  #       identifier that ties its request to the service's own records. It is
  #       declared here once rather than spelled out at each of the twenty-eight
  #       responses, because a header re-typed that many times is a header that
  #       will eventually be typed differently.
  headers:
    XCorrelationId:
      description: >-
        Identity of the unit of work that produced this response. Echoed from the
        request where the caller supplied one and minted by the service where it
        did not, at most twenty-four characters. Quote it when reporting a
        failure; it is the only handle this contract offers into server-side
        diagnostics, which is deliberate -- no response carries an internal
        diagnostic code.
      required: true
      schema:
        $ref: "#/components/schemas/CorrelationId"

  # WHY : Assumptions: the shared kernel's correlation filter reads this header on
  #       the way in and echoes it on the way out, so a caller that already has an
  #       identity for the unit of work it is performing can supply its own and see
  #       its logs and this service's logs carry the same value. Omitting the header
  #       is the ordinary case and the filter mints one, which is why it is optional
  #       rather than required.
  # WHY : Assumptions: the bound and the character rule are the filter's, not this
  #       document's: it accepts at most twenty-four characters, each a letter, a
  #       digit, a hyphen, an underscore or a full stop, and refuses a longer or
  #       otherwise nonconforming value with 400 before any handler runs. Publishing
  #       the rule here is what lets a caller avoid that refusal; restating it
  #       loosely would invite exactly the value the filter rejects.
  # WHY : Trade-offs: the parameter is declared once and referenced by every
  #       operation rather than written out at each. A header re-typed at every
  #       operation is a header that will eventually be typed differently, which is
  #       the same reason the response header beside it is declared once.
  parameters:
    Cursor:
      name: cursor
      in: query
      required: false
      description: >-
        The paging position to continue from: copy the lastKey member of the
        page you hold to advance past it, or its firstKey member to retreat from
        it, and state the matching direction. Omit it, and the direction, for the
        first page. It is opaque and sealed -- a version marker, a nonce and an
        enciphered payload carrying its own authentication tag, separated by full
        stops -- and must be echoed exactly as received; it is not a user identifier and cannot be constructed by a
        caller. The two members it is copied from carry across the baseline's own
        stored page keys at app/cbl/COUSR00C.cbl lines 435 and 389.
      schema:
        $ref: "#/components/schemas/CursorToken"

    PagingDirection:
      name: direction
      in: query
      required: false
      description: >-
        Which side of the supplied cursor to read: next reads keys strictly
        greater than it in ascending order, previous reads keys strictly less than
        it in descending order. It is meaningful only alongside a cursor, and the
        two are sent together or not at all. Defaults to next, which with no
        cursor supplied returns the first page. A direction whose cursor is
        absent, or a cursor sealed for the other direction, is a 400.
      schema:
        $ref: "#/components/schemas/PageDirection"

    # Assumptions: the correlation identity is declared on the REQUEST side as well
    #   as the response, and declaring it on the response side alone would leave the
    #   client's own value checked against nothing. The inbound shape is part of the
    #   contract whether or not the contract states it, because the shared filter
    #   at
    #   services/common-lib/src/main/java/com/carddemo/common/web/CorrelationIdFilter.java
    #   accepts an inbound value and refuses one that is over-long or carries an
    #   unsafe character, and the browser client sends the header on every request
    #   from ui/src/api/client.ts.
    CorrelationIdHeader:
      name: X-Correlation-Id
      in: header
      required: false
      description: >-
        Identity of the unit of work this request belongs to, echoed back on the
        response. Supply it to tie this request to one a caller has already named,
        or omit it and the service mints one. A value longer than the published
        bound, or carrying a character outside the published set, is refused with
        400 rather than being silently replaced, so a caller cannot believe it is
        correlating requests the service is in fact correlating under a different
        identity. A value whose shape is that of a protected identifier -- only digits and the
        three separators, with nine or more digits -- is refused on the same terms, because this
        identity is echoed and logged. The refusal carries the shared problem body
        and the response correlation header, the same as any other 400, and it names
        the rule it applied without ever repeating the value it refused.
      # WHY : Measured: the shared CorrelationId schema admits the EMPTY string, because
      #       ApiError normalises an absent identity to "" at L619 and every problem
      #       document carries the member, so the schema a BODY member names has to admit
      #       it. A REQUEST header must not: conformsWithin in CorrelationIdFilter
      #       declines an empty value and the filter then answers 400. This parameter
      #       therefore narrows the shared schema by one facet instead of restating its
      #       alphabet and width, which would be a second statement of both. Omitting the
      #       header entirely stays valid -- that is governed by required above.
      schema:
        allOf:
          - $ref: "#/components/schemas/CorrelationId"
          - minLength: 1

    UserIdPath:
      name: userId
      in: path
      required: true
      description: >-
        Identifier of the user this request addresses. At most eight characters,
        the width SEC-USR-ID is declared with at app/cpy/CSUSR01Y.cpy line 18 and
        the width the users table stores it at. The value is folded -- trimmed and
        upper-cased -- to derive the key that is looked up, so either case reaches
        the same row.


        Two rules govern the value beyond that width. It must be drawn from the
        printable single-byte range PIC X(08) means, and the key it folds to must
        occupy at most those eight positions; a value failing either is refused with
        400 keyed userId rather than reported as a row that was not found, because a
        value the column could not hold is not a key any row could carry. No
        narrower character class is imposed: the field declares none, so the
        addressable range is kept as wide as an eight-position single-byte key can
        express, and all ten identifiers the seed security file carries -- upper-case
        letters and digits -- sit well inside it.


        No pattern facet is asserted for the range. An expression anchored tightly
        enough to state it would also refuse a segment with a surrounding blank,
        which this service trims rather than rejects, and the same range is stated
        in prose on the request-body property for the same reason -- so the two
        routes into one rule are documented the same way rather than one being
        expressed and the other described.
      schema:
        type: string
        minLength: 1
        maxLength: 8

  # WHY : Trade-offs: only these two are shared, and the others are written out at
  #       each operation even though that is more text. The reason is that every
  #       other failure carries a message literal belonging to a specific program
  #       line, and those literals differ per operation -- the create conflict
  #       says one thing, the delete failure another. Sharing those would have
  #       forced a single generic wording and lost the message fidelity this
  #       contract exists to record. These two carry no baseline literal at all,
  #       because the baseline had no analogue for them: a 3270 user reached these
  #       screens through a signed-on CICS session, so there was no
  #       missing-credential or wrong-authority path to produce a message for.
  responses:
    Unauthorized:
      description: >-
        No bearer token was presented, or the token was rejected -- an invalid
        signature, an expired or not-yet-valid token, the wrong issuer, an
        identity token where an access token is required, a token issued to a
        different app client, or a missing required scope. The body names no
        specific cause, because distinguishing them for an unauthenticated caller
        would describe the validator's configuration to it.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Forbidden:
      description: >-
        The token was valid but its cognito:groups claim does not include
        carddemo-admin, which every user-administration operation requires. This
        is distinct from the 401 above: the caller is known, and it is the
        authority that is missing rather than the credential.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"

    ServiceUnavailable:
      description: >-
        The operation was refused because the environment is not currently
        accepting mutating work. The nightly batch chain owns the data for the
        duration of its window, and NOTHING WAS APPLIED -- the refusal is issued
        before the handler runs, so no part of the request took effect. A caller
        that repeats the same request unchanged once the window reopens succeeds.


        This is the target equivalent of the reference quiesce. app/jcl/CLOSEFIL.jcl
        lines 26 to 30 closed five VSAM files to the CICS region for the duration of
        the posting chain and app/jcl/OPENFIL.jcl reopened them, so a write attempted
        in between received a file-status error and abended. The migrated bracket
        deliberately closes WRITES only, so the reads this service publishes keep
        answering while the window is closed.


        Assumptions: 503 rather than 409 or 403, because the distinction is the one a
        client acts on. The request is well formed and its caller is entitled to make
        it; only the window in which it may be applied is absent. No Retry-After is
        sent, because the window is reopened by a state machine whose remaining work
        this service cannot see, so any value would be a guess a client would then
        schedule against.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"


    # WHY : Refactoring Rationale: the transport refusals below were UNDECLARED while the shared
    #       advice in services/common-lib produces every one of them, so each was reachable on
    #       every operation of this contract with no published shape. A client had no branch for
    #       them: ui/src/api/client.ts could only treat each as an unrecognised body. Declaring
    #       them here and handling them there are two halves of one correction, and declaring one
    #       half alone would have documented a response no caller could recognise.
    # WHY : Assumptions: each carries the same ApiError shape as every other refusal of this
    #       contract, with its own machine code -- CARDDEMO-0405, CARDDEMO-0406 and
    #       CARDDEMO-0415. A distinct code per condition is what lets a client branch without
    #       reading the sentence, which is the same reason the 400 and the 401 do not share one.
    # WHY : Assumptions: the body is declared application/json because that is what the shared
    #       advice writes -- it returns a plain ApiError record and this build registers no
    #       problem+json converter -- and because the 406 arm pins MediaType.APPLICATION_JSON
    #       explicitly, which is the single call that lets a body be produced at all for a caller
    #       whose Accept header matched nothing.
    MethodNotAllowed:
      description: >-
        The route addressed does not publish the method the request named. The response
        carries an Allow header naming the methods it does publish, which is where a client
        reads them; the body does not repeat that list, so the two cannot disagree.


        Assumptions: this has no reference counterpart. A 3270 terminal cannot address a
        transaction with a verb the program did not define, so no reference literal or
        condition name corresponds to this outcome. It exists because a target route is
        addressed by callers this system does not control.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
        Allow:
          description: >-
            The methods the addressed route publishes, joined with a comma and a space.
            Composed from the methods the framework's handler mapping reports, which are
            server-authored; the method the caller USED is echoed nowhere. It is omitted
            rather than sent empty when the framework reported no supported set, because an
            empty Allow states that the route supports no method at all, which would be a
            different and false claim.
          required: false
          schema:
            type: string
            example: "GET, PUT"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"

    UnsupportedMediaType:
      description: >-
        The request carried a body in a media type this route does not accept, or carried a
        body with no Content-Type at all. Every operation of this contract that accepts a
        body accepts application/json and nothing else.


        The body carries no fieldErrors entry and cannot: the refusal is raised before any
        member has been parsed, so there is no bound object to have fields. That is why this
        is a distinct code from CARDDEMO-0400 rather than a 400 with an empty array, which a
        caller could not tell from a body it had mis-spelled.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"

    NotAcceptable:
      description: >-
        The request's Accept header names no media type this service can produce. The shared
        advice answers with JSON and nothing else.


        Assumptions: the body of this response is itself application/json, which is a type
        the caller has just said it will not accept. That is deliberate. The alternative is
        an empty 406, which carries neither the machine code nor the correlation identifier
        a support enquiry needs and tells the caller nothing about which header to change.
        The content type is set explicitly by the shared advice for a second reason recorded
        there: a body left to content negotiation would be negotiated against the same
        Accept header that already matched nothing, and the request would then be forwarded
        to the container error path, which this service publishes no route for.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"


  # WHY : Assumptions: the seven mirrored shapes -- the cursor token, the paging
  #       direction, the correlation identity, the page envelope, the error body,
  #       its per-field entry and its abend aggregate -- describe types published by
  #       services/common-lib and are NOT local redefinitions of them. A contract
  #       cannot reference a Java type, so the shape has to be written out here;
  #       what must never happen is that the written shape drifts from the published
  #       one, because a locally invented error or paging shape would compile, serve
  #       and disagree with the eight services that share the originals. This is the
  #       same discipline the reference suite states for record layouts, where a
  #       layout is resolved through one include path and never duplicated. Every
  #       property name and order below is the shared record's own.
  # WHY : Assumptions: property names are the record component names exactly,
  #       because no property-naming strategy is configured anywhere in this service
  #       or in the shared defaults it imports, so the serialiser emits component
  #       names verbatim. Renaming one here would produce a contract the runtime
  #       contradicts on the first response.
  # WHY : Assumptions: the nine schemas this context owns take their field widths
  #       from two independent readings apiece, the 80-byte record at
  #       app/cpy/CSUSR01Y.cpy lines 17 to 23 and the symbolic maps cited on each
  #       property, which is why the widths are expressed as constraints a caller is
  #       held to rather than as documentation.
  # WHY : Refactoring Rationale: the fixed-length record's padding is not carried
  #       across. SEC-USR-FILLER at app/cpy/CSUSR01Y.cpy line 23 is twenty-three
  #       bytes that exist to make the record eighty bytes long; it addresses no
  #       data, no program reads it, and a JSON payload has no length to pad, so it
  #       appears in no schema.

    # WHY : Refactoring Rationale: a SECOND definition of MethodNotAllowed, NotAcceptable and
    #   UnsupportedMediaType stood here and all three are withdrawn. They were a whole earlier
    #   generation of the same three components, merged alongside the surviving generation, and a
    #   duplicate mapping key is the most deceptive shape this can take: YAML resolves a repeated
    #   key to the LAST definition, so one of every pair was dead text that still read as the
    #   contract, and no schema-shaped test could tell -- both copies were well formed and both
    #   carried the correlation header. The survivors are the generation that states the Allow
    #   header's composition rule with an example and attributes the pinned content type to the
    #   shared advice rather than to a per-service handler; the withdrawn copies said only that
    #   Allow is 'comma separated'. Detected with a duplicate-key-aware composer, because
    #   yaml.safe_load hides the condition by construction.
    PayloadTooLarge:
      description: >-
        The request body exceeds the byte ceiling this service accepts, and
        carries the machine code CARDDEMO-0413. The ceiling is 65536 bytes unless
        the deployment names another under carddemo.web.max-request-body-bytes,
        and it is enforced by a servlet filter ordered ahead of the security
        chain rather than by a handler -- which is why it applies to every
        operation that accepts a body and why the refusal reaches a caller that
        presented no token at all.


        Assumptions: the body of this response carries no fieldErrors entry and
        cannot. The refusal is raised before any member is parsed, so there is no
        bound object to have fields; the message names the ceiling and the size
        the request declared, which is what distinguishes a misconfigured client
        from a deliberate flood.


        Assumptions: a body that declares no length is not refused for that alone.
        It is read up to the ceiling and refused only if it exceeds it, and the
        bytes already read are forwarded to the handler when it does not, so a
        chunked request within the bound is served normally. For that case the
        message states that the body declares no length and exceeds the bound,
        because the exact size is the one thing that was never learned.


        Assumptions: this response carries the correlation identifier like every
        other, and that is a property of filter ORDER rather than of this filter.
        The correlation filter is registered one position ahead of it and has
        already put the identifier on the response before the body is written.
      headers:
        X-Correlation-Id:
          $ref: "#/components/headers/XCorrelationId"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
  schemas:

    SignOnRequest:
      type: object
      description: >-
        The two credential values of the sign-on screen. The password is
        forwarded to the Cognito user pool for the sole purpose of that exchange
        and is never persisted, logged or echoed by this service: the auth
        schema has no column to hold it, and this is the only place in this
        contract where the word appears at all.
      additionalProperties: false
      required: [userId, password]
      properties:
        # Assumptions: the two properties are in the order the sign-on screen
        #   presents and validates them -- identifier then password -- from
        #   app/cpy-bms/COSGN00.CPY line 72 ("02 USERIDI PIC X(8).") and line 78
        #   ("02 PASSWDI PIC X(8)."). The baseline's presence checks run in that
        #   same order at app/cbl/COSGN00C.cbl lines 118 and 123, so a caller
        #   submitting both fields blank is told about the identifier first, and
        #   that is observable.
        # Refactoring Rationale: the NON-WHITESPACE PATTERN closes a gap between
        #   this schema and the Java record that validates against it. An earlier
        #   revision declared minLength 1 alone, which admits a value of eight
        #   spaces; the record's @NotBlank constraint refuses exactly that value.
        #   A caller generating requests from this contract could therefore build a
        #   schema-VALID body that the service answers 400, which is the one class
        #   of disagreement a published contract exists to prevent. The baseline
        #   refuses it too -- app/cbl/COSGN00C.cbl line 118 tests
        #   "= SPACES OR LOW-VALUES" -- so the schema was the side that was wrong.
        # Assumptions: the pattern is UNANCHORED, which is the JSON Schema default
        #   and is deliberate here: "\\S" as a partial match reads "contains at
        #   least one non-whitespace character", which is precisely what @NotBlank
        #   asserts. Anchoring it would instead demand the whole value be a single
        #   non-whitespace character and would refuse every real identifier.
        # Assumptions: this facet must also appear in the OTHER document this
        #   service publishes. The document served from /v3/api-docs is GENERATED
        #   from the Java annotations, and a non-blank constraint contributes a
        #   minimum length and no pattern at all, so a generated description of this
        #   property would be strictly weaker than this committed one and would admit
        #   a value of spaces that both this document and the record refuse. The Java
        #   record publishes the identical string through a schema-documentation
        #   annotation, declared once as SignOnRequest.NON_WHITESPACE_PATTERN, so the
        #   two documents agree character for character.
        # Alternatives Considered: adding a bean-validation @Pattern carrying this
        #   expression, which would have been a runtime constraint rather than a
        #   documentation one. Rejected on two independent grounds. A
        #   bean-validation pattern is matched against the WHOLE value, so "\\S"
        #   there would demand a single non-whitespace character and refuse every
        #   real identifier -- the expression would have had to change here to a form
        #   correct under both readings, altering a published facet. And it would be
        #   REDUNDANT: @NotBlank already refuses exactly the values this pattern
        #   refuses, so a second constraint would add a second violation for one
        #   fault and put two entries for one property into a fieldErrors array this
        #   document declares as one entry per offending field.
        # Trade-offs: the rule is now stated twice, once as minLength and once as
        #   a pattern, and the pattern alone would suffice. Both are kept because
        #   minLength is the constraint a code generator maps to a length check and
        #   the pattern is the one it maps to a regular expression, so a generated
        #   client enforces the rule under either mapping rather than under only
        #   the one this document happened to choose.
        userId:
          type: string
          minLength: 1
          maxLength: 8
          pattern: "\\S"
          description: >-
            The signing-on user's identifier, at most eight characters. Must not
            be blank; the baseline treats SPACES and LOW-VALUES alike as absent
            at app/cbl/COSGN00C.cbl line 118. A value of only whitespace is
            refused by the pattern, matching that check.
        # Refactoring Rationale: the EIGHT-CHARACTER CEILING IS GONE, and its
        #   removal is the security correction this schema carries. An earlier
        #   revision of this document capped the value at eight characters and
        #   attributed the cap to SEC-USR-PWD PIC X(08) at app/cpy/CSUSR01Y.cpy
        #   line 21. That was faithful to a field this migration deliberately does
        #   not carry forward -- V1__auth.sql declares no password column at all,
        #   because the credential moved to the managed user pool -- and it made
        #   the operation unusable rather than merely conservative: the pool's
        #   password policy is configured with a minimum length of 14 and a floor
        #   of 12 that no caller can lower (infra/modules/cognito/variables.tf,
        #   variable password_minimum_length and its validation), so EVERY
        #   password the pool will accept was longer than this contract admitted
        #   and every sign-on would have been refused with 400 before the
        #   credential was ever presented.
        # Assumptions: no minimum length and no character-class requirement is
        #   asserted here even though the pool enforces both -- lowercase,
        #   uppercase, digit and symbol are each required by default in that same
        #   variables file. This operation does not judge a credential's strength;
        #   it forwards it and reports what the pool decided. Restating the policy
        #   here would put it in two places, and the copy in this contract would be
        #   the one that goes stale when an environment tightens the pool, at which
        #   point this service would refuse credentials the pool accepts. The
        #   presence check is retained because it is the baseline's own, at
        #   app/cbl/COSGN00C.cbl line 123, and because forwarding an empty value
        #   would spend a pool call to learn nothing.
        # Trade-offs: an upper bound of 256 is imposed even though the policy's
        #   own maximum is expressed as a minimum-length floor rather than a
        #   ceiling. The bound exists so that an oversized body is refused here
        #   rather than forwarded to the identity provider, and 256 is far enough
        #   above the configured minimum of 14 that no policy this repository can
        #   express would collide with it. The cost is one arbitrary-looking
        #   number; what it buys is that this operation cannot be used to relay
        #   unbounded input to a third party.
        # Assumptions: the same non-whitespace pattern as the identifier
        #   above, for the same reason and from the same baseline check -- the
        #   presence test at app/cbl/COSGN00C.cbl line 123 is the credential's
        #   branch of the identifier's EVALUATE, testing "= SPACES OR LOW-VALUES"
        #   identically. No further character rule is added: the pool's own policy
        #   governs composition, and restating it here would put it in two places.
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 256
          pattern: "\\S"
          writeOnly: true
          description: >-
            The user's password, forwarded to the Cognito user pool for evaluation
            and never persisted, logged or echoed. Must not be blank, per the
            presence check at app/cbl/COSGN00C.cbl line 123; a value of only
            whitespace is refused by the pattern, matching that check. Its length and
            composition are governed by the pool's own password policy, not by
            this contract: a value that fails that policy is reported by the
            outcome the pool returns rather than by field validation here. The
            baseline's eight-character record field is retired along with the
            record column that held it.

    TokenRefreshRequest:
      type: object
      description: >-
        The identifier the tokens were issued for and the refresh token to renew
        them with. Both are required.
      additionalProperties: false
      required: [userId, refreshToken]
      # Refactoring Rationale: both properties carry the non-whitespace pattern the
      #   sign-on identifier records the full argument for. Neither had one while
      #   this schema had no implementing record; TokenRefreshRequest now annotates
      #   both @NotBlank, which refuses exactly what this facet refuses, and
      #   publishes this identical string into the generated document through a
      #   schema-documentation annotation. minLength: 1 alone would have admitted a
      #   token of spaces -- which cannot be a value the pool issued, and which this
      #   service would have relayed to a third party before learning so.
      properties:
        userId:
          type: string
          minLength: 1
          maxLength: 8
          pattern: "\\S"
          description: >-
            The identifier the token set being renewed was issued for, at most eight
            characters. Must not be blank.
        # Refactoring Rationale: this property carried NO maxLength and the comment
        #   here justified the absence by saying an oversized body would be refused by
        #   the transport's own request-size limit instead. Both halves were false. No
        #   such limit existed -- the servlet container's post-size setting bounds form
        #   data, which this operation does not accept, and no service or gateway
        #   configuration named a body size -- so an unbounded JSON body reached the
        #   deserialiser on the one operation in this document that requires no
        #   credential. Throttling limits how OFTEN a caller may call, not how many
        #   bytes one call may carry, so it did not substitute either.
        # Assumptions: the bound is deliberately generous rather than tight, and the
        #   asymmetry is the whole argument. The value is opaque and provider-sized, so
        #   a bound set near the largest token observed would eventually refuse one the
        #   pool had legitimately issued -- which on this operation means locking a
        #   caller out until it signs on again with a credential it may no longer hold.
        #   Accepting a longer invalid value costs only bytes, and those are bounded
        #   before this facet is evaluated at all: the shared kernel's request-body
        #   filter refuses a body above its configured ceiling with 413, before parsing
        #   and before the security chain runs. 8192 is several times the largest token
        #   observed and an eighth of that ceiling, so the two controls stay ordered.
        refreshToken:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: "\\S"
          writeOnly: true
          description: >-
            The refresh token a previous sign-on returned, replayed unaltered. Must
            not be blank, and at most 8192 characters.
            Write-only: it travels in this request only and appears in no response.

    # Assumptions: this schema carries ONE property where TokenRefreshRequest carries
    #   two, and the asymmetry is the provider's rather than an omission. Cognito's
    #   RevokeToken operation takes the token, the client id and the client secret and
    #   accepts no user name -- the token identifies its own subject -- so an identifier
    #   declared here would be read by nothing, and declaring one would tell an
    #   integrator it was checked.
    # Assumptions: the operation makes no membership decision either, which is the
    #   second reason no identifier appears. Refusing to revoke because auth.users no
    #   longer holds a row for the token's subject would leave a removed user's
    #   thirty-day token live, which is the opposite of what a sign-out is for.
    SignOutRequest:
      type: object
      description: >-
        The refresh token to revoke. Required, and the whole of the request.
      additionalProperties: false
      required: [refreshToken]
      properties:
        # Assumptions: the bound is the same 8192 TokenRefreshRequest declares for the
        #   same value, for the reason recorded there -- the value is opaque and
        #   provider-sized, so a generous bound refuses an implausible token without
        #   being able to refuse one the pool legitimately issued, and it stays an
        #   eighth of the shared body ceiling so the two controls remain ordered. A
        #   test asserts the two agree, so a token accepted on one operation cannot be
        #   refused by the other.
        refreshToken:
          type: string
          minLength: 1
          maxLength: 8192
          pattern: "\\S"
          writeOnly: true
          description: >-
            The refresh token to revoke, replayed unaltered. Must not be blank, and at
            most 8192 characters.
            Write-only: it travels in this request only and appears in no response.

    SignOnResponse:
      type: object
      description: >-
        The token set the Cognito user pool minted, with the identifier it was
        minted for. The caller sends accessToken as a bearer credential on every
        other operation in this document.
      additionalProperties: false
      # WHY : Refactoring Rationale: every property below is REQUIRED, including the nullable
      #       ones, and that is a correction rather than a tightening. carddemo-common-defaults.yml
      #       pins default-property-inclusion to always, so every record component is written on
      #       every response and a nullable member arrives as null, NEVER absent. Publishing such
      #       a member as optional described an omission this service cannot produce, and generated
      #       a type that was optional AND nullable -- two states for one fact.
      # WHY : ⚠️ Refactoring Rationale: refreshToken was the member that correction was found on. It
      #       was absent from this list and is now in it: it is a component of the SignOnResponse
      #       record and nothing in this service narrows Jackson's inclusion, so the key is
      #       serialised on every one of these bodies -- as null where the app client issues no
      #       refresh token and on a renewal, which the member's own description already states.
      #       Publishing it as optional conflated that null with an absent key and left a client
      #       provisioning for two states where the wire has one; the ['string', 'null'] union it
      #       carries is what expresses the null, and it is unchanged.
      required: [outcome, userId, accessToken, idToken, refreshToken, tokenType, expiresIn]
      properties:
        # Assumptions: this member exists so that the two things a sign-on can return
        #   are told apart by a VALUE rather than by which members a client happens to
        #   find present. Alternatives Considered: branching on the presence of
        #   accessToken. Rejected -- it makes a missing member load-bearing, which is
        #   exactly the kind of inference a closed schema exists to remove. The
        #   alternative to two success shapes at all is one, which leaves a pool
        #   response that is a CHALLENGE nowhere to go but 500; see the rationale on
        #   SignOnChallenge.
        outcome:
          type: string
          const: AUTHENTICATED
          description: >-
            Always AUTHENTICATED on this shape. It names which of the two sign-on
            outcomes this body carries, and it is the discriminating member of the
            operation's response.
        # Refactoring Rationale: there is deliberately NO user-type property here,
        #   and its absence is the security correction this operation carries. The
        #   baseline put CDEMO-USER-TYPE (app/cpy/COCOM01Y.cpy lines 26 to 28) into
        #   the communication area, which the client echoed back on the following
        #   request, so the value the program branched on was one the client had
        #   most recently held. Returning an equivalent field here would restore
        #   exactly that: a client could send back a type it was never issued.
        #   Authority now lives only in the signed cognito:groups claim, which the
        #   client cannot alter without invalidating the signature.
        # Alternatives Considered: returning the group list as a display-only
        #   convenience, so the browser could pick the administrator menu without
        #   inspecting the token. Rejected because a field shaped like authority
        #   invites use as authority: nothing in a response body can stop a client
        #   branching on it, and a later reader would have no way to tell the
        #   convenience field from the one just removed. The browser already holds
        #   the signed token and reads the claim from it, which is one source
        #   rather than two that could disagree.
        userId:
          type: string
          maxLength: 8
          description: >-
            The identifier the tokens were issued for, echoed so the client can
            render it in the screen title band without decoding a token. It
            carries no authority of any kind.
        accessToken:
          type: string
          description: >-
            The pool's access token, to be sent as the HTTP bearer credential.
            This is the only one of the three tokens this API accepts for
            authorisation.
        idToken:
          type: string
          description: >-
            The pool's identity token, describing the authenticated user to the
            client. It is returned because the browser reads its group claim to
            choose the administrator or general menu, and it is not accepted for
            authorisation by any operation here.
        refreshToken:
          type: ["string", "null"]
          description: >-
            The pool's refresh token where the app client is configured to issue
            one, and null where it is not. On the response to a renewal it carries
            the ROTATED successor the pool issues in place of the token presented,
            which the client must store, because the deployed app client enables
            refresh-token rotation and the presented token is invalidated as it is
            exchanged. It is not a bearer credential and no operation accepts it for
            authorisation; the refresh operation above accepts it in a request body,
            which is the one place it travels.
        tokenType:
          type: string
          description: >-
            The credential scheme the tokens are to be presented under, as the
            pool reports it. Bearer in every currently supported configuration.
          examples: ["Bearer"]
        expiresIn:
          type: integer
          format: int32
          minimum: 1
          description: >-
            Lifetime of accessToken in seconds from issue, as the pool reports it.
            The client re-authenticates or renews before it elapses; this service
            rejects an expired token rather than extending it.

    # Assumptions: this shape and the operation that consumes it are REQUIRED for
    #   the migrated system to be usable at all, so neither may be dropped as an
    #   optional extra. Cognito answers an initial authentication with either a token
    #   set or a CHALLENGE, and one challenge in
    #   particular is unavoidable here: every seed user is created with a temporary
    #   password whose validity is measured in days
    #   (infra/modules/cognito/variables.tf, temporary_password_validity_days), and
    #   a temporary password always yields NEW_PASSWORD_REQUIRED on first use.
    #   Trade-offs: routing that outcome to 500 as an integration failure would make
    #   the very first sign-on of every provisioned user report a server fault with no
    #   path to set a permanent password, which is why the challenge is a contracted
    #   outcome rather than an error. The edge publishes the matching route:
    #   POST /api/v1/auth/challenge is one of the four deliberately unauthenticated
    #   keys in infra/modules/api-gateway-http/variables.tf.
    # Assumptions: the challenge has NO baseline counterpart and none is invented
    #   for it. The reference sign-on compared a stored password directly at
    #   app/cbl/COSGN00C.cbl lines 211 to 256 and had no notion of a credential
    #   that must be changed before use, so there is no message literal to carry
    #   across and none of the three sign-on sentences is reused here: reporting
    #   "Wrong Password. Try again ..." for a challenge would tell an operator to
    #   retype a credential that was in fact correct.
    # Trade-offs: only NEW_PASSWORD_REQUIRED is admitted, not the full set of
    #   challenges the pool can raise. The pool's other challenges belong to the
    #   second-factor configuration, and this document declines to publish a
    #   contract for flows the seeded configuration does not produce; an
    #   unpublished challenge remains a 500, which is honest, because this service
    #   genuinely cannot complete one. The cost is that enabling a second factor
    #   requires an edit here; what it buys is that no client is written against a
    #   challenge exchange that has never been exercised.
    SignOnChallenge:
      type: object
      description: >-
        The sign-on could not complete because the pool requires something further
        of the caller before it will issue tokens. No token of any kind accompanies
        this body. The caller answers it by calling the challenge operation with
        the session value returned here.
      additionalProperties: false
      required: [outcome, challengeName, session, userId]
      properties:
        outcome:
          type: string
          const: CHALLENGE
          description: >-
            Always CHALLENGE on this shape. It names which of the two sign-on
            outcomes this body carries.
        challengeName:
          type: string
          enum: [NEW_PASSWORD_REQUIRED]
          description: >-
            What the pool requires. NEW_PASSWORD_REQUIRED means the credential
            presented was correct but is temporary or expired, and a permanent one
            must be set before tokens are issued. It is the outcome of a first
            sign-on by any user the infrastructure seeded.
        # Trade-offs: the session value is returned to the caller even though it is
        #   a credential of sorts -- possession of it plus a new password completes
        #   the authentication. It has to travel, because the pool requires it back
        #   and this service holds no per-caller state to keep it in; holding it
        #   server-side would reintroduce exactly the session storage that ending
        #   the pseudo-conversational design removed. It is short-lived, single-use
        #   and bound by the pool to the one exchange it was issued for, and it is
        #   write-only in the opposite direction: no operation echoes it back.
        session:
          type: string
          maxLength: 4096
          description: >-
            Opaque continuation value issued by the pool, to be sent back verbatim
            on the challenge operation. It is not a bearer token and is accepted by
            no other operation in this document. Treat it as a credential: do not
            log it, do not store it beyond the exchange, and do not parse it.
        userId:
          type: string
          maxLength: 8
          description: >-
            The identifier the challenge was raised for, echoed so a client can
            render it while collecting the new password. It carries no authority.

    SignOnChallengeRequest:
      type: object
      description: >-
        The answer to a NEW_PASSWORD_REQUIRED challenge: the session value the
        sign-on returned, the identifier it was raised for, and the permanent
        password to set. On success the pool issues the token set the sign-on could
        not.
      additionalProperties: false
      required: [userId, session, newPassword]
      # Refactoring Rationale: all three properties carry the non-whitespace pattern
      #   the sign-on identifier records the full argument for. None had one while
      #   this schema had no implementing record; SignOnChallengeRequest now annotates
      #   all three @NotBlank, which refuses exactly what this facet refuses, and
      #   publishes this identical string into the generated document through a
      #   schema-documentation annotation. minLength: 1 alone would have admitted a
      #   session of 4096 spaces -- which cannot be a value the pool issued, and which
      #   this service would have relayed to a third party before learning so.
      properties:
        userId:
          type: string
          minLength: 1
          maxLength: 8
          pattern: "\\S"
          description: >-
            The identifier the challenge was raised for, as returned on the
            challenge body. Must not be blank. It must match the one the session was
            issued for; the pool refuses the exchange otherwise.
        session:
          type: string
          minLength: 1
          maxLength: 4096
          pattern: "\\S"
          writeOnly: true
          description: >-
            The session value from the challenge body, echoed back verbatim. Must not
            be blank. It is
            single-use: a second attempt with the same value requires a fresh
            sign-on.
        # Assumptions: this member is bounded and present-checked here and its
        #   strength is judged nowhere in this contract, for the same reason
        #   recorded on the sign-on password. The pool applies the configured
        #   policy -- minimum length 14 with a floor of 12, and lowercase,
        #   uppercase, digit and symbol each required, per
        #   infra/modules/cognito/variables.tf -- and a value failing it is
        #   reported through the 400 below carrying the pool's own reason rather
        #   than through a restatement of the policy here that could go stale
        #   against it.
        newPassword:
          type: string
          format: password
          minLength: 1
          maxLength: 256
          pattern: "\\S"
          writeOnly: true
          description: >-
            The permanent password to set. Must not be blank; its length and
            composition are otherwise governed
            by the pool's password policy, not by this contract. Never persisted,
            logged or echoed by this service.

    UserSummary:
      type: object
      description: >-
        One row of the user list, carrying the four values the list screen showed
        per row. It is a projection for browsing, not the full record: a caller
        that needs the Cognito subject reads the individual row.
      additionalProperties: false
      required: [userId, firstName, lastName, userType]
      properties:
        # Assumptions: the four properties are the four the list screen displayed
        #   per row, at the widths app/cpy-bms/COUSR00.CPY declares for its tenth
        #   row group -- USRID10I PIC X(8) at line 348, FNAME10I PIC X(20) at line
        #   354, LNAME10I PIC X(20) at line 360 and UTYPE10I PIC X(1) at line 366.
        #   The tenth group is cited rather than the first because all ten are
        #   identical in shape, so any one of them establishes the widths, and the
        #   tenth additionally establishes the row COUNT: it is the last group the
        #   map declares, which is what makes ten the whole of the page rather
        #   than merely the first ten of something longer.
        # Alternatives Considered: carrying the row's selection marker,
        #   SEL0001I PIC X(1) at app/cpy-bms/COUSR00.CPY line 72 and its nine
        #   siblings, through which the operator typed U or D against a row.
        #   Rejected because it is a screen input rather than data about the user:
        #   it selected an action, and an action is now expressed by the client
        #   calling the update or delete operation on that row's identifier
        #   directly. Carrying it would put a mutable, meaningless field on every
        #   row of every page.
        # Assumptions: the screen's other fields are excluded as terminal chrome
        #   with no analogue -- the shared transaction name, both title lines, the
        #   current date and time, the program name and the leading FILLER that
        #   head all five maps, plus the page-number field. The sign-on map's
        #   APPLIDI at app/cpy-bms/COSGN00.CPY line 60 and SYSIDI at line 66 are
        #   excluded on the same ground: they identify the CICS region and the
        #   system serving the terminal, which no REST response has a counterpart
        #   for.
        userId:
          type: string
          maxLength: 8
          description: >-
            The row's identifier, at most eight characters. It is the value a
            caller puts in the path to read, update or delete that row.
        firstName:
          type: string
          maxLength: 20
          description: >-
            The user's given name, at most twenty characters, from
            SEC-USR-FNAME at app/cpy/CSUSR01Y.cpy line 19. Trailing blanks in the
            fixed-length record were padding and are not part of the value.
        lastName:
          type: string
          maxLength: 20
          description: >-
            The user's family name, at most twenty characters, from SEC-USR-LNAME
            at app/cpy/CSUSR01Y.cpy line 20, with padding likewise excluded.
        userType:
          type: string
          minLength: 1
          maxLength: 1
          enum: ["A", "U"]
          description: >-
            The user's role: A for administrator, U for ordinary user. These two
            are the whole domain, from the condition names on CDEMO-USER-TYPE at
            app/cpy/COCOM01Y.cpy lines 26 to 28, and the users table enforces the
            same pair with a check constraint. A determines whether the account
            is granted the carddemo-admin authority at sign-on, so it is
            administrative data rather than a display attribute.


    # Refactoring Rationale: the create and update bodies are two separate schemas
    #   and one is NOT derived from the other, even though the update body's three
    #   properties are a subset of the create body's five. The baseline validated
    #   its screens field by field and stopped at the first fault, in physical
    #   screen order, so which error a user is shown when several fields are wrong
    #   is observable behaviour and it genuinely differs between the two screens.
    #   Create runs first name, last name, identifier, then type
    #   (app/cbl/COUSR01C.cbl lines 120, 126, 132 and 144, over the map order at
    #   app/cpy-bms/COUSR01.CPY lines 60, 66, 72 and 84). Update runs identifier,
    #   first name, last name, password, then type (app/cbl/COUSR02C.cbl lines 182,
    #   188, 194, 200 and 206, over app/cpy-bms/COUSR02.CPY lines 60, 66, 72, 78
    #   and 84); the identifier has become the path parameter and the password has
    #   gone with the credential, leaving lines 188, 194 and 206 as exactly the
    #   three that survive. Deriving one schema from the other with a composition
    #   keyword would merge two orders into one and silently change which fault a
    #   caller is told about first on one of the two screens.
    # Alternatives Considered: a single request schema with the identifier
    #   marked optional, used by both operations. Rejected on one count beyond the
    #   ordering above: it would make the identifier optional on create where it
    #   is mandatory, and accepted on update where the path already carries it, so
    #   a body could name a different user than the path and the contract would
    #   have to say which wins. Two schemas cannot express that contradiction at
    #   all.

    CreateUserRequest:
      type: object
      description: >-
        The values a new user row is created from. There is deliberately no
        password property: the baseline stored an eight-character password in the
        clear at app/cpy/CSUSR01Y.cpy line 21, compared it in the clear at
        app/cbl/COSGN00C.cbl line 223, and wrote it back onto the update screen at
        app/cbl/COUSR02C.cbl line 169, and the migrated design ends all three by
        making the Cognito user pool the sole holder of the credential. This
        service therefore creates no credential, and the sibling migration
        db/migration/V1__auth.sql has no column any password property here could
        be stored in.


        There is also deliberately no cognitoSub property. This service creates
        the pool account and its group membership from the four values below and
        reads the subject out of the provider's response, so the subject is an
        output of creating a user rather than an input to it; it appears on the
        201 response and nowhere in this schema. additionalProperties is false, so
        a body that names it is refused with 400 rather than silently ignored.
      additionalProperties: false
      required: [firstName, lastName, userId, userType]
      # Assumptions: the three character properties below carry pattern "\\S", and
      #   minLength: 1 with a prose sentence would NOT be enough. Under minLength
      #   alone a value of twenty spaces is schema-VALID and service-INVALID, because
      #   CreateUserRequest annotates all three @NotBlank and refuses it -- so a
      #   caller generating a request from this document could build a body the
      #   document accepts and the service answers 400, which is exactly the
      #   disagreement a published contract exists to prevent. Prose cannot close it;
      #   only a machine-readable constraint can.
      # Assumptions: the pattern is UNANCHORED, the JSON Schema default, so "\\S"
      #   as a partial match reads "contains at least one non-whitespace
      #   character" -- precisely what @NotBlank asserts. Anchoring it would demand
      #   the whole value be one non-whitespace character and would refuse every
      #   real name. This is the same form and the same reasoning the sign-on
      #   schema above already uses, stated once per schema because a generator
      #   reads each schema independently. The record implementing this body
      #   publishes the identical string into the generated document through a
      #   schema-documentation annotation, so the two documents agree; the reason
      #   that is a documentation annotation rather than a second runtime
      #   constraint is recorded on the sign-on identifier above.
      # Assumptions: userType needs no pattern. Its enum admits exactly "A" and
      #   "U", neither of which is blank, so the domain constraint already refuses
      #   every whitespace value and a pattern beside it would restate a rule the
      #   enum states more precisely.
      properties:
        firstName:
          type: string
          minLength: 1
          maxLength: 20
          pattern: "\\S"
          description: >-
            The new user's given name, at most twenty characters. Must not be
            blank; validated first, per app/cbl/COUSR01C.cbl line 120. A value of
            only whitespace is refused by the pattern, matching that check.
        lastName:
          type: string
          minLength: 1
          maxLength: 20
          pattern: "\\S"
          description: >-
            The new user's family name, at most twenty characters. Must not be
            blank; validated second, per app/cbl/COUSR01C.cbl line 126. A value of
            only whitespace is refused by the pattern, matching that check.
        userId:
          type: string
          minLength: 1
          maxLength: 8
          pattern: "\\S"
          description: >-
            Identifier for the new row and its primary key, at most eight
            characters. Must not be blank, per app/cbl/COUSR01C.cbl line 132 -- a
            value of only whitespace is refused by the pattern, matching that
            check -- and must not already exist: a duplicate is refused with 409
            rather than overwriting the existing row.


            The value is folded -- trimmed and upper-cased -- to derive the stored
            key, and two further rules govern that derivation. A value carrying a
            character outside the printable single-byte range PIC X(08) means
            (app/cpy/CSUSR01Y.cpy line 18) is refused, and so is one whose derived
            key occupies more than those eight positions. The first rule is what
            makes maxLength above sufficient for the second: upper-casing is
            length-preserving only within that range -- eight sharp-s characters
            are eight positions submitted and sixteen once folded -- so without it
            a value satisfying maxLength could still exceed the positions the
            column stores. Both refusals are 400 keyed userId and both are raised
            before the duplicate check and before any pool account is created, so a
            refused identifier leaves nothing behind.


            Neither rule is published as a pattern facet, and the reason is that
            this property already carries one. JSON Schema admits a single pattern
            per property, the one declared here refuses a whitespace-only value to
            match the record's non-blank constraint, and the character range cannot
            be folded into it: an expression anchored tightly enough to state the
            range would also refuse a value with a surrounding blank, which this
            service trims rather than rejects. The rules are therefore stated here
            in prose for both routes into them, rather than expressed for one and
            described for the other.
        userType:
          type: string
          minLength: 1
          maxLength: 1
          enum: ["A", "U"]
          description: >-
            The new user's role, A for administrator or U for ordinary user, the
            whole domain per app/cpy/COCOM01Y.cpy lines 26 to 28. Must not be
            blank, per app/cbl/COUSR01C.cbl line 144; the enum carries that,
            because neither admitted value is blank. Choosing A grants that user
            the carddemo-admin authority at sign-on, including the authority to
            reach these operations.
        # Refactoring Rationale: the pool subject is deliberately NOT a property of
        #   this request. It is the ONLY link between a presented token and a row --
        #   auth.users declares the column NOT NULL UNIQUE -- so a caller able to
        #   nominate it would be a caller able to decide which pool identity a new
        #   row authenticates as. An administrator could bind a row to another
        #   person's subject, or to a subject whose pool account holds the
        #   administrator group while the row records 'U'; the signed group claim
        #   wins every authorization decision, so the row's own type would be
        #   decoration. A well-formed UUID is the whole of what a schema could
        #   check, so neither case would be detectable.
        # Assumptions: excluding it is only half of what makes the link safe. The
        #   service creates the pool account and its group membership from the four
        #   properties above and reads the subject back from the provider's own
        #   response, so the row and the pool identity are established by one act
        #   and cannot drift. The subject is returned on the 201 as part of
        #   UserResponse, so a caller learns which subject its row was bound to.
        # Alternatives Considered: keeping the property and validating it against
        #   the pool -- reading the named subject back and refusing one that does
        #   not exist or whose group disagrees with userType. Rejected because it
        #   answers a question that should not be asked: it costs a round trip per
        #   request against a value the service can produce, it still admits a real
        #   subject belonging to a different person, and it leaves an account
        #   provisioned by some unnamed other party as a precondition of using this
        #   operation at all.

    UpdateUserRequest:
      type: object
      description: >-
        The three values an existing user row may change. Two absences are
        deliberate. There is no identifier property, because the identifier is the
        primary key and travels in the request path: accepting it in the body too
        would allow a request whose path and body named different users, and the
        contract would then have to arbitrate. There is no password property, for
        the reason given on the create body -- which also means the baseline's
        "Password can NOT be empty..." branch (app/cbl/COUSR02C.cbl line 200, and
        its twin at app/cbl/COUSR01C.cbl line 138) has no counterpart anywhere in
        this contract, since the service neither accepts nor stores a password on
        create or update. That branch is recorded here as removed rather than left
        to look overlooked.
      additionalProperties: false
      required: [firstName, lastName, userType]
      # Refactoring Rationale: the two name properties carry the non-whitespace
      #   pattern for the same reason the create body's do -- each description
      #   asserted the value must not be blank while minLength: 1 beside it admitted
      #   a value of twenty spaces, so the document contradicted itself.
      # Assumptions: com.carddemo.auth.dto.UpdateUserRequest annotates both names
      #   @NotBlank, which refuses exactly what this facet refuses, and publishes this
      #   identical string into the GENERATED document through a schema-documentation
      #   annotation. That second half is load-bearing: a non-blank constraint on its
      #   own renders as a minimum length and no pattern, so without the annotation the
      #   generated document and this one would disagree on this single facet. The
      #   expression is stated once in Java, on
      #   SignOnRequest.NON_WHITESPACE_PATTERN.
      properties:
        firstName:
          type: string
          minLength: 1
          maxLength: 20
          pattern: "\\S"
          description: >-
            The user's given name, at most twenty characters. Must not be blank;
            validated first among the three, per app/cbl/COUSR02C.cbl line 188. A
            value of only whitespace is refused by the pattern, matching that
            check.
        lastName:
          type: string
          minLength: 1
          maxLength: 20
          pattern: "\\S"
          description: >-
            The user's family name, at most twenty characters. Must not be blank;
            validated second, per app/cbl/COUSR02C.cbl line 194. A value of only
            whitespace is refused by the pattern, matching that check.
        userType:
          type: string
          minLength: 1
          maxLength: 1
          enum: ["A", "U"]
          description: >-
            The user's role, A for administrator or U for ordinary user. Must not
            be blank; validated third, per app/cbl/COUSR02C.cbl line 206, and the
            enum carries that because neither admitted value is blank. Changing it
            grants or removes that user's carddemo-admin authority at their next
            sign-on, so it is the one field here with an authorisation
            consequence.


            The group move behind it is applied to the identity provider AFTER this
            operation's transaction commits, so a 200 means the row is stored and
            the move is owed rather than that the provider has already been
            reached. The service records the owed move durably and retries it until
            the provider agrees, so the two converge without a further request; a
            reader of this row within that interval may see a role the provider has
            not yet moved. This is stated because the alternative -- calling the
            provider inside the transaction -- cannot be atomic: the provider does
            not roll back, so a successful move followed by a failed commit would
            leave the move applied and this row unchanged, with nothing recording
            it.

    CreatedUserResponse:
      type: object
      description: >-
        The body of a successful create: the five stored columns exactly as
        UserResponse publishes them, plus credentialSecretName -- the NAME of the
        managed-secret entry the new pool account's one-time credential was published
        to. The credential itself is NOT here, and the Refactoring Rationale below
        records why.


        Assumptions: the five row properties are REPEATED here rather than composed
        from UserResponse with allOf. UserResponse declares
        additionalProperties: false, and a composition that adds a property to a
        schema which forbids additional properties is ill-defined -- several
        validators refuse every instance of it. Repeating the properties keeps this
        body a strict superset of the read body, so a client that ignores the new
        property reads exactly what it read before.


        Refactoring Rationale: NO credential crosses the wire in this response. What
        this body adds to the read body is the NAME of the managed-secret entry the
        credential was published to, and three properties of the credential itself
        keep the handover safe from there. It is single-use: the account stands in
        the provider's force-change state, so presenting it raises the new-password
        challenge and can do nothing else. It is never persisted:
        db/migration/V1__auth.sql declares no password column, deliberately,
        undoing SEC-USR-PWD PIC X(08) at app/cpy/CSUSR01Y.cpy line 21 which the
        baseline stored in the clear, compared in the clear at
        app/cbl/COSGN00C.cbl line 223 and wrote back onto a screen at
        app/cbl/COUSR02C.cbl line 169. And it is never logged: both the response
        record and the provisioning result override their diagnostic rendering to
        withhold what they hold.


        Alternatives Considered: answering with the generated credential itself.
        Rejected because a credential in a response body is copied into every proxy
        log and browser history between the service and the operator, and the
        migration forbids a credential reaching a place with no audit trail; the
        value is published to a per-user managed-secret entry encrypted with the
        customer-managed key instead, and this body names that entry. Alternatives
        Considered: answering with the read body and nothing else, leaving the name
        to be recomputed from the identifier. Rejected because that tells an operator
        the derivation rule rather than the answer, while a locator is not a secret.


        Trade-offs: reading the entry needs secretsmanager:GetSecretValue and the
        key's grant, which the task role holds and a browser session does not, so
        onboarding ends with one privileged read. That is accepted: it is an audited
        step in a store that already has rotation and an audit trail.
      additionalProperties: false
      required:
        [userId, firstName, lastName, userType, cognitoSub, credentialSecretName]
      properties:
        userId:
          type: string
          maxLength: 8
          description: >-
            The row's identifier and primary key, at most eight characters, with the
            column's blank padding already removed.
        firstName:
          type: string
          maxLength: 20
          description: >-
            The user's given name, at most twenty characters.
        lastName:
          type: string
          maxLength: 20
          description: >-
            The user's family name, at most twenty characters.
        userType:
          type: string
          minLength: 1
          maxLength: 1
          enum: ["A", "U"]
          description: >-
            The user's role, A for administrator or U for ordinary user, per
            app/cpy/COCOM01Y.cpy lines 26 to 28.
        cognitoSub:
          type: string
          format: uuid
          description: >-
            The Cognito subject identifier the row is bound to, minted by the
            provider when it created the account.
        credentialSecretName:
          type: string
          minLength: 1
          description: >-
            The name of the managed-secret entry holding the one-time credential the
            account was created with. Collect the value from that entry and present it
            once at first sign-on: the account is in the provider's force-change
            state, so POST /api/v1/auth/signon answers with the new-password challenge
            and POST /api/v1/auth/challenge completes the exchange. The credential
            itself never appears in this body.


            Assumptions: the property is REQUIRED, not optional. An absent locator
            would mean an account nobody can reach, and a client handling both shapes
            could not tell that from an omission of its own. No format is declared,
            because the value is a resource name rather than a secret; minLength is a
            floor rather than the derived length, which is deliberately not published
            -- the derivation is a property of the service and this contract should not
            invite a client to validate against it.

    UserResponse:
      type: object
      description: >-
        A complete stored user row, returned by the read, create and update
        operations so that one client type covers all three. It carries the five
        columns db/migration/V1__auth.sql defines and nothing else -- notably no
        password, which that migration has no column for.
      additionalProperties: false
      required: [userId, firstName, lastName, userType, cognitoSub]
      properties:
        userId:
          type: string
          maxLength: 8
          description: >-
            The row's identifier and primary key, at most eight characters. Stored
            blank-padded to a declared width; the padding is a storage artefact and
            not part of the value.
        firstName:
          type: string
          maxLength: 20
          description: >-
            The user's given name, at most twenty characters.
        lastName:
          type: string
          maxLength: 20
          description: >-
            The user's family name, at most twenty characters.
        userType:
          type: string
          minLength: 1
          maxLength: 1
          enum: ["A", "U"]
          description: >-
            The user's role, A for administrator or U for ordinary user, per
            app/cpy/COCOM01Y.cpy lines 26 to 28. This is the stored role of the
            row being described; it says nothing about the authority of the caller
            that requested it, which comes from that caller's own token.
        cognitoSub:
          type: string
          format: uuid
          description: >-
            The Cognito subject identifier this row is bound to. Returned so an
            administrator can confirm which pool account a row represents, which
            is the only way to tell two similarly named users apart.


    CursorToken:
      type: string
      maxLength: 256
      pattern: '^v2\.[A-Za-z0-9_-]{16}\.[A-Za-z0-9_-]{1,200}$'
      description: >-
        An opaque, authenticated, ENCIPHERED, expiring position in an ordered
        result set, minted by this service and echoed back by the caller unchanged.
        Its three period-separated parts are a version marker, a nonce and an
        enciphered payload carrying its own authentication tag; a caller must not
        parse, edit, decode or construct one, because base64url-decoding it yields
        ciphertext and because the tag covers both that ciphertext and a binding
        naming the query, the subject, the narrowing filter and the direction the
        token was issued for, so an altered or borrowed token fails to decipher and
        is refused with 400. It is bound to the DIRECTION it was
        minted for as well, so replaying the previous-page position with direction
        next cannot silently answer with the wrong page. The shape is declared once
        here and referenced by the single cursor parameter and by both cursor
        members of the page envelope, so none of the three can acquire a different
        bound. It is a string in every direction and never a number.

    # Assumptions: the two member values are the two the already-authored browser
    #   client sends, declared as the union "next" | "previous" at
    #   ui/src/api/cards.ts, and the two the sibling card and transaction contracts
    #   publish for the same purpose, so there is one paging vocabulary across this
    #   migration. They are declared as a named schema rather than inline on the
    #   parameter so that a generated client names the type once.
    PageDirection:
      type: string
      description: >-
        Which side of a supplied cursor to read. Reading next seeks keys strictly
        greater than the cursor in ascending order; reading previous seeks keys
        strictly less than it in descending order, which is the migrated form of
        the baseline READPREV at app/cbl/COUSR00C.cbl line 343.
      enum: [next, previous]
      default: next

    # Refactoring Rationale: ONE bound and ONE character set for the correlation
    #   identity, declared once and referenced by the request parameter, the
    #   response header and the error body alike. Three descriptions of one value
    #   existed across this migration's three contracts -- twenty-four characters
    #   here, twenty in the transaction contract and unbounded with
    #   thirty-six-character examples in the card contract -- and nothing compared
    #   them, so the disagreement was invisible until a request was refused.
    # Assumptions: twenty-four characters, and the set of letters, digits, hyphen,
    #   underscore and full stop, are not chosen here. They are what
    #   CorrelationIdFilter.java enforces -- CORRELATION_ID_MAX_LENGTH is 24 at its
    #   line 238 and its accepted punctuation is exactly those three characters --
    #   so a value outside that is refused with 400 before any handler runs, and
    #   publishing anything wider would document a request this service rejects.
    # Assumptions: the empty string is admitted as well as a conforming identity,
    #   and it is not slack: the shared error shape normalises an absent identity to
    #   the empty string, which is the state of a failure raised before the
    #   correlation filter ran. Declaring only the token shape would describe a
    #   body this service can in fact emit as invalid.
    CorrelationId:
      type: string
      maxLength: 24
      pattern: '^$|^(?!(?:[._-]*[0-9]){9,}[._-]*$)[A-Za-z0-9_.-]{1,24}$'
      description: >-
        Identity of one unit of work. At most twenty-four characters drawn from the
        ASCII letters, the digits, the hyphen, the underscore and the full stop. A
        value the service mints is exactly the two-character prefix CD followed by twenty-two upper-case hexadecimal
        characters over eleven bytes of entropy; a value a caller supplies need
        only fit the bound and the set. The empty string appears only inside an
        error body, and only for a failure raised before any identity had been
        established. The pattern additionally refuses one narrow class the character set alone
        admits: a value made up only of digits and the three separators, carrying nine
        or more digits. That is the shape of every protected identifier this system
        holds, in contiguous form and in every separated form -- a customer identifier
        and a national identifier are nine digits, an account identifier eleven, a
        card number sixteen -- and the shared correlation filter refuses it because
        this identity is echoed on the response and written to every log line for the
        request, so accepting one would copy a protected identifier into both.
        Ordinary values are unaffected: a date-like 2022-07-18 carries eight digits
        and is accepted, and any value containing a letter is accepted whatever its
        digits. A value the service mints begins CD, which is a non-numeric prefix
        chosen so that a minted identity can never fall into the refused class.
      examples: ["4F1C0E423A554D219B7E6C0F"]

    PageResponse:
      type: object
      description: >-
        One page of users, positioned by key, together with everything needed to
        continue in either direction. A page holds at most ten rows -- the invariant
        size described on hasNext below -- and there is no total count of rows or
        pages anywhere in this envelope, because supplying one would require
        counting the whole table on every request that only ever returns ten rows.
      additionalProperties: false
      required: [items, firstKey, lastKey, hasNext]
      properties:
        # Assumptions: these five properties, in this order, are the five components
        #   of the shared page envelope services/common-lib publishes, so what a
        #   caller receives here is that record serialised and nothing else. The
        #   transaction and card contracts declare the same five under the same
        #   required list, so one client type reads a page from any of the three.
        # Alternatives Considered: SEVEN properties -- the two boundary tokens, a
        #   separate forward and backward scan position, and a backward availability
        #   indicator. Rejected: four components is the shape this migration fixed for
        #   the envelope, the shared record declares exactly those four, and a
        #   contract does not reinterpret the plan it implements. Nothing the three
        #   extra properties would carry is lost -- a page whose rows were all
        #   filtered away after being read carries its two scan positions in firstKey
        #   and lastKey, which is what they are on such a page, and backward
        #   availability is the presence of firstKey.
        items:
          type: array
          maxItems: 10
          description: >-
            The rows of this page in ascending user-identifier order, at most ten.
            Never null: an exhausted page, or one whose rows were all filtered
            away, carries an empty array instead.
          items:
            $ref: "#/components/schemas/UserSummary"
        # Refactoring Rationale: both boundary tokens are taken from the rows this
        #   page actually returned, which is a correction to the baseline's
        #   behaviour on a short page. The baseline wrote its last-row cursor only
        #   on the tenth row of a full page (app/cbl/COUSR00C.cbl line 435) and its
        #   first-row cursor only on the first (line 389), so the second through
        #   ninth rows wrote no cursor at all and a final page of fewer than ten
        #   rows left the stored last-row value pointing at whatever the previous
        #   full page had left behind. Deriving both from the first and last
        #   returned row makes a short page as navigable as a full one. The
        #   divergence is documented, not fixed in the baseline: no COBOL is
        #   altered.
        firstKey:
          description: >-
            Sealed cursor naming this page's leading boundary: the first row
            returned in items, or -- on a page whose every row was filtered away
            after being read -- the key at which the backward scan stopped. Null
            when neither exists. A caller sends this value back as cursor, with
            direction BACKWARD, to read the page before this one, so its presence is
            what tells a caller a backward step is expressible.
          oneOf:
            - $ref: "#/components/schemas/CursorToken"
            - type: "null"
        lastKey:
          description: >-
            Sealed cursor naming this page's trailing boundary: the last row
            returned in items -- the last row the caller actually received, never
            the surplus row read to settle hasNext -- or, on a filtered-away page,
            the key at which the forward scan stopped. Null when neither exists. A
            caller sends this value back as cursor, with direction FORWARD, to read
            the page after this one, and it is present whenever hasNext is true.
          oneOf:
            - $ref: "#/components/schemas/CursorToken"
            - type: "null"
        # Assumptions: forward availability is discovered by reading one row beyond
        #   the page and observing whether that row exists -- never from a count of
        #   rows or pages, which this envelope does not carry. That is precisely
        #   the baseline's own mechanism: after filling its ten-row screen it
        #   issued one further read at app/cbl/COUSR00C.cbl line 311 and set its
        #   indicator true at line 313 when a row came back, false at line 315 when
        #   the read failed, and false again on the separate end-of-file path at
        #   line 318. The page size of ten is set by the contract and is not a
        #   caller's choice, on two independent readings: the screen's row table is
        #   "02 USER-REC OCCURS 10 TIMES." at app/cbl/COUSR00C.cbl line 57, and the
        #   map itself declares exactly ten row groups at a thirty-line stride,
        #   from SEL0001I at app/cpy-bms/COUSR00.CPY line 72 through SEL0010I at
        #   line 342 with that row's four data fields at lines 348, 354, 360 and
        #   366.
        hasNext:
          type: boolean
          description: >-
            Whether a further page follows this one. False on the last page and
            false when this page is empty because the set is exhausted. A caller
            binds the forward paging control to this value, which is what the
            baseline's own next-page indicator expressed, and it is never true
            without a lastKey to continue from.

    # WHY : (1) Assumptions: these three describe the shared error record, its
    #       per-field entry and its abend aggregate exactly as
    #       services/common-lib publishes them, including property order, so a
    #       client written against this contract handles the errors of all eight
    #       services. (2) Assumptions: no internal diagnostic is exposed by any of
    #       them, and that is a boundary rather than an omission. The baseline
    #       programs write their CICS response and reason codes to the operator
    #       log alongside the user's message -- at app/cbl/COUSR00C.cbl lines 608,
    #       642 and 676, app/cbl/COUSR02C.cbl lines 347 and 384, and
    #       app/cbl/COUSR03C.cbl lines 294 and 330 -- and those codes stay on the
    #       server side of this contract. They describe which platform mechanism
    #       failed, which tells a caller nothing it can act on and tells an
    #       attacker something it can. The correlation identifier is what crosses
    #       the boundary instead: it ties the caller's report to the server's own
    #       record of the same failure. (3) Assumptions: every response of every
    #       operation, failing or succeeding, is application/json. The shared
    #       advice that builds this shape returns ResponseEntity<ApiError> and sets
    #       no content type of its own, so the framework negotiates application/json
    #       and that is what a caller actually receives.
    #       Alternatives Considered: declaring the failing responses as
    #       application/problem+json. Rejected twice over. It names a media type no
    #       handler emits, so a client routing on content type before parsing would
    #       take the wrong branch on every failure; and this shape is not an RFC 9457
    #       problem document -- it declares
    #       code, secondaryCode, severity, subsystem, status, correlationId, path,
    #       timestamp, fieldErrors and abend, and none of the type, title, detail
    #       and instance members that specification requires. Declaring the media
    #       type the handler emits is what this document does; restructuring the
    #       shared shape into a problem document was also rejected, because it is
    #       published by
    #       services/common-lib for all eight services and the two sibling
    #       contracts already describe it as application/json.
    # WHY : ⚠️ Refactoring Rationale: these three domains are declared as named components
    #   because they belong to ONE shared record that every context writes, and this
    #   document previously wrote their values out inline inside `ApiError` and
    #   `FieldError`. Six sibling contracts already publish them under exactly these
    #   names, so the inline form made this the only document from which a generated
    #   client produced anonymous local enumerations for values that cross every
    #   service boundary. Naming them also gives the values one place to change.
    # WHY : Assumptions: each enumeration reproduces the Java domain it is generated
    #   from exactly -- the four severity constants, the six subsystem constants and the
    #   two field states declared in
    #   services/common-lib/src/main/java/com/carddemo/common/error/ApiError.java and the
    #   shared validation package. A document listing a value the writer cannot emit
    #   would have a client branch on an unreachable state; one omitting a value the
    #   writer can emit would have a strict client refuse a well-formed body.
    Severity:
      type: string
      enum: [LOG, INFO, WARNING, CRITICAL]
      example: WARNING
      description: >-
        The operational level of a failure, drawn from the baseline's own four-value
        domain rather than a newly invented ladder, so an operator reading a migrated
        failure sees the level the platform would have reported. The client-correctable
        failures this context reports are at the warning level, because each is an
        outcome a caller may act on rather than a fault in the service; the unclassified
        failure reported as 500 is at the critical level.

    Subsystem:
      type: string
      enum: [APPLICATION, CICS, IMS, RELATIONAL, QUEUE, OBJECT_STORE]
      example: APPLICATION
      description: >-
        Which subsystem the failure is attributed to, using the baseline's own
        vocabulary so an operator can route a migrated failure the way the platform's
        would have been routed. This context reports APPLICATION for its own validation
        and business refusals and RELATIONAL for a store failure; the remaining values
        exist because the record is shared with contexts that do reach those subsystems.

    FieldValidationState:
      type: string
      enum: [NOT_OK, BLANK]
      example: BLANK
      description: >-
        Why a field appears in the per-field list: NOT_OK for a value that was supplied
        and rejected, BLANK for one that was absent or blank. The two stay distinct
        because the baseline treated them differently on screen -- a blank field
        additionally received a literal asterisk marker, which is the disjunction at
        app/cpy/CSSETATY.cpy lines 18 and 19 with the marker written at lines 23 to 25.
        The shared validation domain declares a third, acceptable state; it cannot
        appear here, because an entry exists only where a field was refused.

    ApiError:
      type: object
      description: >-
        A failure, described in the shape every CardDemo service returns. It
        carries a stable code to match on, a human-readable message reproduced
        from the originating baseline program where that failure has one, the
        offending fields where the failure is attributable to fields, and the
        correlation identifier to quote when reporting it.
      additionalProperties: false
      # WHY : Refactoring Rationale: `message` and `abend` are REQUIRED and NULLABLE, which is a
      #       different statement from optional and is the one this shape has to make. Both are record
      #       components of ApiError in
      #       services/common-lib/src/main/java/com/carddemo/common/error/ApiError.java, and a record
      #       component is always written, so the key is present with a null value on every response --
      #       `ApiErrorWireShapeTest` asserts exactly that, emitting `"message":null` and `"abend":null`.
      #       An earlier revision omitted them from this list, which told a generated client the keys
      #       might be missing and left it unable to distinguish "not supplied" from "nothing to
      #       report"; a strict validator reading `additionalProperties: false` alongside the omission
      #       had no member to bind the emitted keys to at all.
      # WHY : Alternatives Considered: suppressing the two keys when null, with an inclusion setting, so
      #       the omission became true. Rejected because the aggregate sentence is the reference's own
      #       75-character message field and a client renders the band from it unconditionally, so a
      #       key that comes and goes moves that decision into every client; and because
      #       always-inclusion is PINNED for every service in
      #       services/common-lib/src/main/resources/carddemo-common-defaults.yml, which the
      #       money contract SEPARATELY depends on -- a money member with no value must be
      #       present and null rather than absent, because absent reads as "no balance" where
      #       null reads as "no amount applies". That is a second consumer of the same pin and
      #       NOT the reason these two keys are required here: the reason is the record shape
      #       above, and this schema carries no monetary member at all. An earlier revision ran
      #       the two together, which read as though a problem document's nullability followed
      #       from a money rule.
      # WHY : ⚠️ Refactoring Rationale: that last clause used to read "the services set
      #       default-property-inclusion: always" and it named no place, which is how it came
      #       to be inaccurate -- exactly one of the seven services actually set the key, and
      #       the other six, this one included, rested a published contractual promise on the
      #       serialiser's own default. The key is now set once in the shared defaults every
      #       service imports, so the claim is true for all of them and is verifiable by
      #       reading one file. The reasoning is unchanged; only its subject is now real.
      required: [code, secondaryCode, message, severity, subsystem, status, correlationId, path, timestamp, fieldErrors, abend]
      properties:
        # WHY : Assumptions: the bound of 13 is the exact width of every identifier the
        #   shared writer can emit, not a round number -- each is the literal
        #   `CARDDEMO-` followed by four digits, so all eight values are 13 characters.
        #   Publishing it lets a client size a fixed field for the value; leaving it
        #   unbounded described an open-ended string for a closed, uniform set.
        code:
          type: string
          maxLength: 13
          description: >-
            Stable machine-readable identifier for the failure, matched on by
            clients and by operational tooling and never reworded. The values this
            context produces are CARDDEMO-0400 for a validation failure,
            CARDDEMO-0401 for an absent or unacceptable credential, CARDDEMO-0403
            for a credential lacking the required authority, CARDDEMO-0404 for an
            absent row, CARDDEMO-0405 for a method the addressed route does not
            publish, CARDDEMO-0406 for an Accept header naming no type this service
            produces, CARDDEMO-0409 for a duplicate identifier, CARDDEMO-0415 for a
            body in a media type this service does not read, CARDDEMO-0500 for an
            internal failure and CARDDEMO-0503 for a request refused while writes
            are quiesced.


            Assumptions: the list names one code per refusal status this document
            declares, so a client that branches on the code can tell when it has
            seen them all. Five were missing from it -- the credential pair, which
            this contract has always declared, and the three transport codes added
            with the 405, 406 and 415 responses below -- and an incomplete list of
            this kind reads as though it were complete.
        secondaryCode:
          type: string
          maxLength: 22
          description: >-
            Subordinate identifier qualifying code where the failure has a second
            classification, and the empty string where it has none. Empty rather
            than absent, so a client need not distinguish a missing property from
            an unqualified failure.
        # Assumptions: this property is genuinely nullable and null does NOT mean
        #   the same thing as the empty string. The baseline can distinguish a
        #   message field that was never set from one deliberately set to blanks,
        #   so the shared record keeps both states: null is message-off and empty
        #   is message-present-but-blank. Collapsing them would merge two
        #   observable conditions.
        # Assumptions: message text is carried character for character from the
        #   program line it originates on, including spacing before an ellipsis,
        #   which differs per program and even within one program and is therefore
        #   never normalised -- see the note in this file's header.
        # Assumptions: the bound is 75, the shared rendering width
        #   ApiError.MESSAGE_RENDERING_WIDTH declares from
        #   CCARD-ERROR-MSG PIC X(75) and CCARD-RETURN-MSG PIC X(75) at
        #   app/cpy/CVCRD01Y.cpy lines 28 and 29. Alternatives Considered: 78, the
        #   width the message field occupies in all five auth maps
        #   (app/cpy-bms/COSGN00.CPY line 84, COUSR00.CPY line 372, COUSR01.CPY
        #   line 90, COUSR02.CPY line 90 and COUSR03.CPY line 84). Both widths are
        #   real, but one record is shared by every service and cannot be described
        #   at two widths at once: a client reading three contracts would see three
        #   bounds on one property. The shared constant wins, and it costs nothing
        #   -- the longest string carried here is 44 characters and the longest the
        #   shared advice can emit is 58, so no message is shortened to fit either
        #   width. Trade-offs: the 78-character screen field is recorded here rather
        #   than dropped, because it is the width a renderer reproducing the 3270
        #   presentation has to allow for.
        message:
          type: ["string", "null"]
          maxLength: 75
          description: >-
            The human-readable sentence for this failure, reproduced verbatim from
            its originating program where one exists, or null where the failure
            has no message. The per-operation responses above name the exact
            literal each status carries, together with the program and line it was
            read from.
        # WHY : ⚠️ Refactoring Rationale: the four-value domain is referenced from a named
        #   component rather than written out here, and it was written out here. Six
        #   sibling contracts name the same domain as `Severity` and reference it; this
        #   document was the only one restating the values inline, so one shared record's
        #   domain was expressed two ways across seven documents that describe it. A
        #   generated client built from this document produced a differently-named local
        #   enumeration from one built from any sibling, for a value that crosses every
        #   context. Referencing also means adding a level is one edit rather than seven.
        severity:
          $ref: '#/components/schemas/Severity'
          description: >-
            Operational level of the failure, drawn from the baseline's own
            four-value domain rather than a newly invented ladder, so an operator
            reading a migrated failure sees the level the platform would have
            reported.
        subsystem:
          $ref: '#/components/schemas/Subsystem'
          description: >-
            Which part of the platform the failure arose in, from the baseline's
            own six-value domain. Failures from this context are APPLICATION or
            RELATIONAL; the remaining values exist because the enumeration is
            shared with services whose failures can arise elsewhere, and pruning
            it per service would give one name six different meanings.
          examples: ["APPLICATION"]
        status:
          type: integer
          format: int32
          minimum: 400
          maximum: 599
          description: >-
            The HTTP status this response carries, restated inside the body so
            that a logged or archived payload is self-describing once separated
            from its response envelope.
        correlationId:
          allOf:
            - $ref: "#/components/schemas/CorrelationId"
          description: >-
            Identity of the unit of work that failed, the same value as the
            X-Correlation-Id response header, and the empty string if the request
            carried none. Quote it when reporting a failure: it is the handle that
            ties this response to the server-side record, which is what makes
            withholding internal diagnostics workable rather than merely opaque.
        path:
          type: string
          description: >-
            The request path that failed, so a client reporting a problem need not
            reconstruct which call produced it. The empty string where no path is
            available.
        # WHY : ⚠️ Assumptions: the shape is published as a REGULAR EXPRESSION and not as
        #   `format: date-time`, because the value is not an RFC 3339 instant. It is the
        #   baseline's 26-character stamp -- a space between date and time rather than a
        #   `T`, six fractional digits, and no zone designator -- emitted by the shared
        #   TimestampFormatter from the pattern `uuuu-MM-dd HH:mm:ss.SSSSSS`. Declaring
        #   `date-time` would tell a generated client to parse it as an instant, and a
        #   strict parser refuses this value for the missing `T` and offset while a
        #   lenient one invents a zone. The bound and the expression together pin the
        #   width the reference field carried.
        timestamp:
          type: string
          maxLength: 26
          pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}\.[0-9]{6}$'
          description: >-
            The instant of the failure in the baseline-compatible
            twenty-six-character form, to microsecond precision, and the empty
            string where no reading was supplied. It is the time the failure was
            recorded and is not read from the wall clock when the body is
            assembled.
        fieldErrors:
          type: array
          description: >-
            One entry per offending field, in the order the validating code
            reported them, which is the baseline's physical screen order -- so the
            first entry is the fault a 3270 user would have been shown first.
            Never null: it is an empty array when the failure is not attributable
            to particular fields, as with a conflict or an internal failure.
          items:
            $ref: "#/components/schemas/FieldError"
        abend:
          description: >-
            The structured abend aggregate when the failure was an abend, and
            null otherwise. Nullable rather than an empty object because such a
            failure genuinely has no abend detail rather than an empty one. The
            member itself is never absent: the pinned inclusion policy writes
            every record component, so a caller reads one state, not two.
          oneOf:
            - $ref: "#/components/schemas/AbendDetail"
            - type: "null"

    FieldError:
      type: object
      description: >-
        One offending field within a failed request: which field, what was wrong
        with it, and the message the baseline showed for it.
      additionalProperties: false
      required: [field, state, message]
      properties:
        # Assumptions: the key is the logical field name, derived from where the
        #   baseline homed the cursor on that branch but not identical to it. The
        #   cursor targets are per-branch and are not always the field at fault --
        #   app/cbl/COUSR02C.cbl homes to the first-name field on its catch-all arm
        #   at lines 210 to 212 regardless of which value was rejected -- so
        #   reproducing the target would sometimes blame the wrong field. The keys
        #   this context produces are userId, password, firstName, lastName,
        #   userType and confirmed.
        # WHY : Assumptions: the lower bound is stated because an entry exists only when a
        #   named field was refused, so an empty name is not a value this array can
        #   carry -- a client keying its per-field highlight off the name would have
        #   nothing to key on. Three sibling contracts already bound it this way.
        field:
          type: string
          minLength: 1
          description: >-
            Name of the offending field as it appears in the request: a property of
            the request body, a path parameter, or a query parameter. The names
            this context produces are userId, password, firstName, lastName,
            userType and confirmed.
        # Assumptions: TWO states, not three. The shared validation domain in
        #   com.carddemo.common.validation declares a third, acceptable state,
        #   migrated from the baseline's per-field flag convention, but an entry in
        #   this array exists only because a field was refused, so the acceptable
        #   state cannot appear in one. The shared advice bears this out: it
        #   constructs an entry with the rejected state or the blank state and with
        #   nothing else. BLANK stays distinct from NOT_OK because the baseline
        #   treated the two differently on screen -- a blank field additionally
        #   received a literal asterisk marker, whereas a rejected value did not --
        #   which is the disjunction at app/cpy/CSSETATY.cpy lines 18 and 19 with the
        #   marker written at lines 23 to 25.
        # Alternatives Considered: publishing the acceptable state here too, on the
        #   ground that the enumeration is the shared one. Rejected -- a published
        #   member no response can carry describes a state a client must write a branch
        #   for and will never reach, and it would disagree with the sibling contracts,
        #   which publish two.
        state:
          $ref: '#/components/schemas/FieldValidationState'
          description: >-
            Why the field is listed: NOT_OK for a value that was supplied and
            rejected, BLANK for one that was absent or blank. A caller rendering
            the baseline's presentation shows the asterisk marker for BLANK only.
        # Refactoring Rationale: the same 75 as the aggregate message above, and for
        #   the same reason: this is the nested entry of one shared record, so the
        #   two cannot be bounded differently in one contract and identically in
        #   its siblings. The longest literal any of these five programs produces
        #   for a field is well inside it.
        message:
          type: string
          minLength: 1
          maxLength: 75
          description: >-
            The message this field's failure produced, carried verbatim from its
            originating program line, spacing included. The per-operation responses
            above list the exact literal each field can produce with its source
            line.

    AbendDetail:
      type: object
      description: >-
        The externally safe projection of an abend. Its four properties mirror the
        ABEND-DATA structure at app/cpy/CSMSG02Y.cpy lines 21 to 29, but only the
        first carries a value: the shared error record projects an abend to this
        safe form before serialising it, so the culprit and reason are blanked and
        the message is replaced with a constant sentence. That is deliberate -- the
        culprit names an internal program and the reason carries a platform
        diagnostic, and neither is something a client can act on.
      additionalProperties: false
      required: [abendCode, abendCulprit, abendReason, abendMsg]
      properties:
        abendCode:
          type: string
          maxLength: 4
          description: >-
            The abend code, at most four characters, from ABEND-CODE at
            app/cpy/CSMSG02Y.cpy line 22. The one component retained in the
            external projection, because it classifies the failure without naming
            any internal component.
        abendCulprit:
          type: string
          maxLength: 8
          description: >-
            Always the empty string in a response. The property exists because the
            shared record's shape includes it, mirroring ABEND-CULPRIT at
            app/cpy/CSMSG02Y.cpy line 24, but the external projection blanks it
            rather than naming the failing internal program.
        abendReason:
          type: string
          maxLength: 50
          description: >-
            Always the empty string in a response, for the same reason as
            abendCulprit. It mirrors ABEND-REASON at app/cpy/CSMSG02Y.cpy line 26,
            whose value is a platform diagnostic that stays on the server side of
            this contract.
        abendMsg:
          type: string
          maxLength: 72
          description: >-
            A constant sentence asking the caller to quote the correlation identifier
            when reporting the failure. It occupies the position of ABEND-MSG at
            app/cpy/CSMSG02Y.cpy line 28 and replaces whatever that field held,
            so the width is preserved while the internal text is not disclosed.
